# Static Dashboard Server

This folder hosts the Material UI dashboard static page.

The page is served by the existing FastAPI backend in `backend/server.py`.

## Files

- `index.html`: dashboard page

## Run

From the repository root:

```bash
python3 backend/server.py
```

Then open:

- http://127.0.0.1:8000/dashboard
