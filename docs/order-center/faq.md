---
domain: order-center
type: faq
faq: true
version: v1.0
summary: 解释用户下单后一直不支付时，系统如何通过延迟消息自动关闭订单。说明订单号必须使用雪花算法生成，不能直接暴露数据库自增主键。说明订单取消的状态边界，以及取消后状态会如何流转。解释延迟 MQ 发送时长与订单超时取消检查的关系。
keywords:
  - 未支付订单
  - 超时取消
  - RocketMQ
  - 订单号生成
  - 雪花 ID
  - 自增 ID
  - 订单取消
  - INIT
  - CANCELED
  - 延迟 MQ
  - 15分钟
  - 库存释放
sections:
  - title: 未支付订单处理：15 分钟延迟 MQ 与超时取消
  - title: 订单号生成：为什么不能使用数据库自增 ID
  - title: 允许取消的状态：仅 INIT 可取消
  - title: 延迟消息用途：触发 INIT 订单超时检查
acl:
  allow:
    - "$authenticated"
---

# 订单中心 FAQ：订单号、取消状态与超时处理

## 未支付订单处理：15 分钟后会自动取消吗？
系统会先创建状态为 `INIT` 的订单，并发送一条延迟 15 分钟的 RocketMQ 消息。
如果 15 分钟内仍未收到支付成功回调，系统会触发“超时取消”机制，将订单自动关闭并释放库存，不会长期占用库存。

## 订单号生成：为什么不能使用数据库自增 ID？
不能。
订单号必须通过 `IdGenerator.nextSnowflakeId()` 生成 64 位雪花算法 ID，不能直接暴露数据库自增 ID，否则会泄露业务规模和订单量信息。

## 允许取消的状态：为什么只有 INIT 能取消？
只有处于 `INIT`（初始化 / 待支付）状态的订单才允许取消。
取消成功后，订单状态会流转为 `CANCELED`。

## 创单 ID 规范：为什么必须使用雪花算法？
绝对不能使用数据库自增 ID，以防泄露商业数据量法则。必须调用分布式 ID 生成服务 `IdGenerator.nextSnowflakeId()` 获取雪花算法订单号。

## 延迟消息用途：15 分钟消息用来做什么？
必须向 RocketMQ 发送一条延迟 15 分钟的 MQ 消息。该消息用于触发处于 `INIT` 状态订单的“超时未支付自动取消”死信检查逻辑，以释放底层库存。