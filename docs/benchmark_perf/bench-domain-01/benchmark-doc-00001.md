---
title: Benchmark Document 00001
domain: bench-domain-01
type: reference
summary: Synthetic benchmark document 00001 for bench-domain-01. Tokens include BENCH_01_00001_001 BENCH_01_00001_002 BENCH_01_00001_003 BENCH_01_00001_004 BENCH_01_00001_005.
keywords:
  - BENCH_01_00001_001
  - BENCH_01_00001_002
  - BENCH_01_00001_003
  - BENCH_01_00001_004
  - BENCH_01_00001_005
  - BENCH_01_00001_006
  - BENCH_01_00001_007
  - BENCH_01_00001_008
  - BENCH_01_00001_009
  - BENCH_01_00001_010
  - BENCH_01_00001_011
  - BENCH_01_00001_012
  - BENCH_01_00001_013
  - BENCH_01_00001_014
  - BENCH_01_00001_015
  - BENCH_01_00001_016
  - BENCH_01_00001_017
  - BENCH_01_00001_018
  - BENCH_01_00001_019
  - BENCH_01_00001_020
  - BENCH_01_00001_021
  - BENCH_01_00001_022
  - BENCH_01_00001_023
  - BENCH_01_00001_024
  - BENCH_01_00001_025
  - BENCH_01_00001_026
  - BENCH_01_00001_027
  - BENCH_01_00001_028
  - BENCH_01_00001_029
  - BENCH_01_00001_030
  - BENCH_01_00001_031
  - BENCH_01_00001_032
  - BENCH_01_00001_033
  - BENCH_01_00001_034
  - BENCH_01_00001_035
  - BENCH_01_00001_036
  - BENCH_01_00001_037
  - BENCH_01_00001_038
  - BENCH_01_00001_039
  - BENCH_01_00001_040
  - bench-domain-01
  - benchmark
related_domains:
  - bench-domain-01
  - global
acl: public
---

# Benchmark Document 00001

This generated document belongs to `bench-domain-01` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_01_00001_001

Benchmark 标识 `BENCH_01_00001_001` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/001`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_001`、`metric_001_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_01_00001_002

Benchmark 标识 `BENCH_01_00001_002` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/002`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_002`、`metric_002_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_01_00001_003

Benchmark 标识 `BENCH_01_00001_003` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/003`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_003`、`metric_003_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_01_00001_004

Benchmark 标识 `BENCH_01_00001_004` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/004`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_004`、`metric_004_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_01_00001_005

Benchmark 标识 `BENCH_01_00001_005` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/005`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_005`、`metric_005_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_01_00001_006

Benchmark 标识 `BENCH_01_00001_006` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/006`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_006`、`metric_006_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_01_00001_007

Benchmark 标识 `BENCH_01_00001_007` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/007`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_007`、`metric_007_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_01_00001_008

Benchmark 标识 `BENCH_01_00001_008` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/008`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_008`、`metric_008_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_01_00001_009

Benchmark 标识 `BENCH_01_00001_009` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/009`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_009`、`metric_009_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_01_00001_010

Benchmark 标识 `BENCH_01_00001_010` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/010`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_010`、`metric_010_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_01_00001_011

Benchmark 标识 `BENCH_01_00001_011` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/011`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_011`、`metric_011_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_01_00001_012

Benchmark 标识 `BENCH_01_00001_012` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/012`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_012`、`metric_012_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_01_00001_013

Benchmark 标识 `BENCH_01_00001_013` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/013`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_013`、`metric_013_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_01_00001_014

Benchmark 标识 `BENCH_01_00001_014` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/014`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_014`、`metric_014_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_01_00001_015

Benchmark 标识 `BENCH_01_00001_015` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/015`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_015`、`metric_015_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_01_00001_016

Benchmark 标识 `BENCH_01_00001_016` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/016`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_016`、`metric_016_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_01_00001_017

Benchmark 标识 `BENCH_01_00001_017` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/017`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_017`、`metric_017_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_01_00001_018

Benchmark 标识 `BENCH_01_00001_018` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/018`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_018`、`metric_018_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_01_00001_019

Benchmark 标识 `BENCH_01_00001_019` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/019`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_019`、`metric_019_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_01_00001_020

Benchmark 标识 `BENCH_01_00001_020` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/020`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_020`、`metric_020_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_01_00001_021

Benchmark 标识 `BENCH_01_00001_021` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/021`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_021`、`metric_021_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_01_00001_022

Benchmark 标识 `BENCH_01_00001_022` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/022`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_022`、`metric_022_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_01_00001_023

Benchmark 标识 `BENCH_01_00001_023` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/023`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_023`、`metric_023_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_01_00001_024

Benchmark 标识 `BENCH_01_00001_024` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/024`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_024`、`metric_024_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_01_00001_025

Benchmark 标识 `BENCH_01_00001_025` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/025`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_025`、`metric_025_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_01_00001_026

Benchmark 标识 `BENCH_01_00001_026` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/026`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_026`、`metric_026_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_01_00001_027

Benchmark 标识 `BENCH_01_00001_027` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/027`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_027`、`metric_027_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_01_00001_028

Benchmark 标识 `BENCH_01_00001_028` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/028`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_028`、`metric_028_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_01_00001_029

Benchmark 标识 `BENCH_01_00001_029` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/029`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_029`、`metric_029_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_01_00001_030

Benchmark 标识 `BENCH_01_00001_030` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/030`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_030`、`metric_030_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_01_00001_031

Benchmark 标识 `BENCH_01_00001_031` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/031`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_031`、`metric_031_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_01_00001_032

Benchmark 标识 `BENCH_01_00001_032` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/032`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_032`、`metric_032_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_01_00001_033

Benchmark 标识 `BENCH_01_00001_033` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/033`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_033`、`metric_033_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_01_00001_034

Benchmark 标识 `BENCH_01_00001_034` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/034`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_034`、`metric_034_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_01_00001_035

Benchmark 标识 `BENCH_01_00001_035` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/035`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_035`、`metric_035_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_01_00001_036

Benchmark 标识 `BENCH_01_00001_036` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/036`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_036`、`metric_036_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_01_00001_037

Benchmark 标识 `BENCH_01_00001_037` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/037`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_037`、`metric_037_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_01_00001_038

Benchmark 标识 `BENCH_01_00001_038` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/038`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_038`、`metric_038_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_01_00001_039

Benchmark 标识 `BENCH_01_00001_039` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/039`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_039`、`metric_039_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_01_00001_040

Benchmark 标识 `BENCH_01_00001_040` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00001/040`。
核心表 `t_bench_bench_domain_01_01` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B001040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00001_040`、`metric_040_value`、`t_bench_bench_domain_01_01` 三个关键词是否同时出现在上下文。
