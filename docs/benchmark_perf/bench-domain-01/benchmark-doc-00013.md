---
title: Benchmark Document 00013
domain: bench-domain-01
type: reference
summary: Synthetic benchmark document 00013 for bench-domain-01. Tokens include BENCH_01_00013_001 BENCH_01_00013_002 BENCH_01_00013_003 BENCH_01_00013_004 BENCH_01_00013_005.
keywords:
  - BENCH_01_00013_001
  - BENCH_01_00013_002
  - BENCH_01_00013_003
  - BENCH_01_00013_004
  - BENCH_01_00013_005
  - BENCH_01_00013_006
  - BENCH_01_00013_007
  - BENCH_01_00013_008
  - BENCH_01_00013_009
  - BENCH_01_00013_010
  - BENCH_01_00013_011
  - BENCH_01_00013_012
  - BENCH_01_00013_013
  - BENCH_01_00013_014
  - BENCH_01_00013_015
  - BENCH_01_00013_016
  - BENCH_01_00013_017
  - BENCH_01_00013_018
  - BENCH_01_00013_019
  - BENCH_01_00013_020
  - BENCH_01_00013_021
  - BENCH_01_00013_022
  - BENCH_01_00013_023
  - BENCH_01_00013_024
  - BENCH_01_00013_025
  - BENCH_01_00013_026
  - BENCH_01_00013_027
  - BENCH_01_00013_028
  - BENCH_01_00013_029
  - BENCH_01_00013_030
  - BENCH_01_00013_031
  - BENCH_01_00013_032
  - BENCH_01_00013_033
  - BENCH_01_00013_034
  - BENCH_01_00013_035
  - BENCH_01_00013_036
  - BENCH_01_00013_037
  - BENCH_01_00013_038
  - BENCH_01_00013_039
  - BENCH_01_00013_040
  - bench-domain-01
  - benchmark
related_domains:
  - bench-domain-01
  - global
acl: public
---

# Benchmark Document 00013

This generated document belongs to `bench-domain-01` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_01_00013_001

Benchmark 标识 `BENCH_01_00013_001` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/001`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_001`、`metric_001_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_01_00013_002

Benchmark 标识 `BENCH_01_00013_002` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/002`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_002`、`metric_002_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_01_00013_003

Benchmark 标识 `BENCH_01_00013_003` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/003`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_003`、`metric_003_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_01_00013_004

Benchmark 标识 `BENCH_01_00013_004` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/004`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_004`、`metric_004_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_01_00013_005

Benchmark 标识 `BENCH_01_00013_005` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/005`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_005`、`metric_005_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_01_00013_006

Benchmark 标识 `BENCH_01_00013_006` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/006`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_006`、`metric_006_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_01_00013_007

Benchmark 标识 `BENCH_01_00013_007` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/007`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_007`、`metric_007_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_01_00013_008

Benchmark 标识 `BENCH_01_00013_008` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/008`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_008`、`metric_008_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_01_00013_009

Benchmark 标识 `BENCH_01_00013_009` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/009`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_009`、`metric_009_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_01_00013_010

Benchmark 标识 `BENCH_01_00013_010` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/010`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_010`、`metric_010_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_01_00013_011

Benchmark 标识 `BENCH_01_00013_011` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/011`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_011`、`metric_011_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_01_00013_012

Benchmark 标识 `BENCH_01_00013_012` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/012`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_012`、`metric_012_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_01_00013_013

Benchmark 标识 `BENCH_01_00013_013` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/013`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_013`、`metric_013_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_01_00013_014

Benchmark 标识 `BENCH_01_00013_014` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/014`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_014`、`metric_014_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_01_00013_015

Benchmark 标识 `BENCH_01_00013_015` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/015`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_015`、`metric_015_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_01_00013_016

Benchmark 标识 `BENCH_01_00013_016` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/016`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_016`、`metric_016_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_01_00013_017

Benchmark 标识 `BENCH_01_00013_017` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/017`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_017`、`metric_017_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_01_00013_018

Benchmark 标识 `BENCH_01_00013_018` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/018`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_018`、`metric_018_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_01_00013_019

Benchmark 标识 `BENCH_01_00013_019` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/019`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_019`、`metric_019_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_01_00013_020

Benchmark 标识 `BENCH_01_00013_020` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/020`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_020`、`metric_020_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_01_00013_021

Benchmark 标识 `BENCH_01_00013_021` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/021`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_021`、`metric_021_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_01_00013_022

Benchmark 标识 `BENCH_01_00013_022` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/022`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_022`、`metric_022_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_01_00013_023

Benchmark 标识 `BENCH_01_00013_023` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/023`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_023`、`metric_023_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_01_00013_024

Benchmark 标识 `BENCH_01_00013_024` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/024`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_024`、`metric_024_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_01_00013_025

Benchmark 标识 `BENCH_01_00013_025` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/025`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_025`、`metric_025_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_01_00013_026

Benchmark 标识 `BENCH_01_00013_026` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/026`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_026`、`metric_026_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_01_00013_027

Benchmark 标识 `BENCH_01_00013_027` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/027`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_027`、`metric_027_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_01_00013_028

Benchmark 标识 `BENCH_01_00013_028` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/028`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_028`、`metric_028_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_01_00013_029

Benchmark 标识 `BENCH_01_00013_029` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/029`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_029`、`metric_029_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_01_00013_030

Benchmark 标识 `BENCH_01_00013_030` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/030`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_030`、`metric_030_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_01_00013_031

Benchmark 标识 `BENCH_01_00013_031` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/031`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_031`、`metric_031_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_01_00013_032

Benchmark 标识 `BENCH_01_00013_032` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/032`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_032`、`metric_032_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_01_00013_033

Benchmark 标识 `BENCH_01_00013_033` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/033`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_033`、`metric_033_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_01_00013_034

Benchmark 标识 `BENCH_01_00013_034` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/034`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_034`、`metric_034_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_01_00013_035

Benchmark 标识 `BENCH_01_00013_035` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/035`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_035`、`metric_035_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_01_00013_036

Benchmark 标识 `BENCH_01_00013_036` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/036`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_036`、`metric_036_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_01_00013_037

Benchmark 标识 `BENCH_01_00013_037` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/037`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_037`、`metric_037_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_01_00013_038

Benchmark 标识 `BENCH_01_00013_038` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/038`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_038`、`metric_038_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_01_00013_039

Benchmark 标识 `BENCH_01_00013_039` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/039`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_039`、`metric_039_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_01_00013_040

Benchmark 标识 `BENCH_01_00013_040` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00013/040`。
核心表 `t_bench_bench_domain_01_13` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B013040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00013_040`、`metric_040_value`、`t_bench_bench_domain_01_13` 三个关键词是否同时出现在上下文。
