---
title: Benchmark Document 00014
domain: bench-domain-02
type: reference
summary: Synthetic benchmark document 00014 for bench-domain-02. Tokens include BENCH_02_00014_001 BENCH_02_00014_002 BENCH_02_00014_003 BENCH_02_00014_004 BENCH_02_00014_005.
keywords:
  - BENCH_02_00014_001
  - BENCH_02_00014_002
  - BENCH_02_00014_003
  - BENCH_02_00014_004
  - BENCH_02_00014_005
  - BENCH_02_00014_006
  - BENCH_02_00014_007
  - BENCH_02_00014_008
  - BENCH_02_00014_009
  - BENCH_02_00014_010
  - BENCH_02_00014_011
  - BENCH_02_00014_012
  - BENCH_02_00014_013
  - BENCH_02_00014_014
  - BENCH_02_00014_015
  - BENCH_02_00014_016
  - BENCH_02_00014_017
  - BENCH_02_00014_018
  - BENCH_02_00014_019
  - BENCH_02_00014_020
  - BENCH_02_00014_021
  - BENCH_02_00014_022
  - BENCH_02_00014_023
  - BENCH_02_00014_024
  - BENCH_02_00014_025
  - BENCH_02_00014_026
  - BENCH_02_00014_027
  - BENCH_02_00014_028
  - BENCH_02_00014_029
  - BENCH_02_00014_030
  - BENCH_02_00014_031
  - BENCH_02_00014_032
  - BENCH_02_00014_033
  - BENCH_02_00014_034
  - BENCH_02_00014_035
  - BENCH_02_00014_036
  - BENCH_02_00014_037
  - BENCH_02_00014_038
  - BENCH_02_00014_039
  - BENCH_02_00014_040
  - bench-domain-02
  - benchmark
related_domains:
  - bench-domain-02
  - global
acl: public
---

# Benchmark Document 00014

This generated document belongs to `bench-domain-02` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_02_00014_001

Benchmark 标识 `BENCH_02_00014_001` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/001`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_001`、`metric_001_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_02_00014_002

Benchmark 标识 `BENCH_02_00014_002` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/002`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_002`、`metric_002_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_02_00014_003

Benchmark 标识 `BENCH_02_00014_003` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/003`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_003`、`metric_003_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_02_00014_004

Benchmark 标识 `BENCH_02_00014_004` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/004`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_004`、`metric_004_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_02_00014_005

Benchmark 标识 `BENCH_02_00014_005` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/005`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_005`、`metric_005_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_02_00014_006

Benchmark 标识 `BENCH_02_00014_006` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/006`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_006`、`metric_006_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_02_00014_007

Benchmark 标识 `BENCH_02_00014_007` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/007`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_007`、`metric_007_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_02_00014_008

Benchmark 标识 `BENCH_02_00014_008` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/008`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_008`、`metric_008_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_02_00014_009

Benchmark 标识 `BENCH_02_00014_009` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/009`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_009`、`metric_009_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_02_00014_010

Benchmark 标识 `BENCH_02_00014_010` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/010`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_010`、`metric_010_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_02_00014_011

Benchmark 标识 `BENCH_02_00014_011` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/011`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_011`、`metric_011_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_02_00014_012

Benchmark 标识 `BENCH_02_00014_012` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/012`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_012`、`metric_012_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_02_00014_013

Benchmark 标识 `BENCH_02_00014_013` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/013`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_013`、`metric_013_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_02_00014_014

Benchmark 标识 `BENCH_02_00014_014` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/014`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_014`、`metric_014_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_02_00014_015

Benchmark 标识 `BENCH_02_00014_015` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/015`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_015`、`metric_015_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_02_00014_016

Benchmark 标识 `BENCH_02_00014_016` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/016`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_016`、`metric_016_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_02_00014_017

Benchmark 标识 `BENCH_02_00014_017` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/017`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_017`、`metric_017_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_02_00014_018

Benchmark 标识 `BENCH_02_00014_018` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/018`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_018`、`metric_018_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_02_00014_019

Benchmark 标识 `BENCH_02_00014_019` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/019`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_019`、`metric_019_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_02_00014_020

Benchmark 标识 `BENCH_02_00014_020` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/020`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_020`、`metric_020_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_02_00014_021

Benchmark 标识 `BENCH_02_00014_021` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/021`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_021`、`metric_021_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_02_00014_022

Benchmark 标识 `BENCH_02_00014_022` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/022`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_022`、`metric_022_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_02_00014_023

Benchmark 标识 `BENCH_02_00014_023` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/023`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_023`、`metric_023_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_02_00014_024

Benchmark 标识 `BENCH_02_00014_024` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/024`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_024`、`metric_024_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_02_00014_025

Benchmark 标识 `BENCH_02_00014_025` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/025`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_025`、`metric_025_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_02_00014_026

Benchmark 标识 `BENCH_02_00014_026` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/026`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_026`、`metric_026_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_02_00014_027

Benchmark 标识 `BENCH_02_00014_027` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/027`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_027`、`metric_027_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_02_00014_028

Benchmark 标识 `BENCH_02_00014_028` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/028`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_028`、`metric_028_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_02_00014_029

Benchmark 标识 `BENCH_02_00014_029` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/029`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_029`、`metric_029_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_02_00014_030

Benchmark 标识 `BENCH_02_00014_030` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/030`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_030`、`metric_030_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_02_00014_031

Benchmark 标识 `BENCH_02_00014_031` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/031`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_031`、`metric_031_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_02_00014_032

Benchmark 标识 `BENCH_02_00014_032` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/032`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_032`、`metric_032_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_02_00014_033

Benchmark 标识 `BENCH_02_00014_033` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/033`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_033`、`metric_033_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_02_00014_034

Benchmark 标识 `BENCH_02_00014_034` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/034`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_034`、`metric_034_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_02_00014_035

Benchmark 标识 `BENCH_02_00014_035` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/035`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_035`、`metric_035_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_02_00014_036

Benchmark 标识 `BENCH_02_00014_036` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/036`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_036`、`metric_036_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_02_00014_037

Benchmark 标识 `BENCH_02_00014_037` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/037`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_037`、`metric_037_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_02_00014_038

Benchmark 标识 `BENCH_02_00014_038` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/038`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_038`、`metric_038_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_02_00014_039

Benchmark 标识 `BENCH_02_00014_039` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/039`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_039`、`metric_039_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_02_00014_040

Benchmark 标识 `BENCH_02_00014_040` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00014/040`。
核心表 `t_bench_bench_domain_02_14` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B014040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00014_040`、`metric_040_value`、`t_bench_bench_domain_02_14` 三个关键词是否同时出现在上下文。
