---
title: Benchmark Document 00005
domain: bench-domain-05
type: reference
summary: Synthetic benchmark document 00005 for bench-domain-05. Tokens include BENCH_05_00005_001 BENCH_05_00005_002 BENCH_05_00005_003 BENCH_05_00005_004 BENCH_05_00005_005.
keywords:
  - BENCH_05_00005_001
  - BENCH_05_00005_002
  - BENCH_05_00005_003
  - BENCH_05_00005_004
  - BENCH_05_00005_005
  - BENCH_05_00005_006
  - BENCH_05_00005_007
  - BENCH_05_00005_008
  - BENCH_05_00005_009
  - BENCH_05_00005_010
  - BENCH_05_00005_011
  - BENCH_05_00005_012
  - BENCH_05_00005_013
  - BENCH_05_00005_014
  - BENCH_05_00005_015
  - BENCH_05_00005_016
  - BENCH_05_00005_017
  - BENCH_05_00005_018
  - BENCH_05_00005_019
  - BENCH_05_00005_020
  - BENCH_05_00005_021
  - BENCH_05_00005_022
  - BENCH_05_00005_023
  - BENCH_05_00005_024
  - BENCH_05_00005_025
  - BENCH_05_00005_026
  - BENCH_05_00005_027
  - BENCH_05_00005_028
  - BENCH_05_00005_029
  - BENCH_05_00005_030
  - BENCH_05_00005_031
  - BENCH_05_00005_032
  - BENCH_05_00005_033
  - BENCH_05_00005_034
  - BENCH_05_00005_035
  - BENCH_05_00005_036
  - BENCH_05_00005_037
  - BENCH_05_00005_038
  - BENCH_05_00005_039
  - BENCH_05_00005_040
  - bench-domain-05
  - benchmark
related_domains:
  - bench-domain-05
  - global
acl: public
---

# Benchmark Document 00005

This generated document belongs to `bench-domain-05` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_05_00005_001

Benchmark 标识 `BENCH_05_00005_001` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/001`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_001`、`metric_001_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_05_00005_002

Benchmark 标识 `BENCH_05_00005_002` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/002`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_002`、`metric_002_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_05_00005_003

Benchmark 标识 `BENCH_05_00005_003` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/003`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_003`、`metric_003_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_05_00005_004

Benchmark 标识 `BENCH_05_00005_004` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/004`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_004`、`metric_004_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_05_00005_005

Benchmark 标识 `BENCH_05_00005_005` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/005`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_005`、`metric_005_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_05_00005_006

Benchmark 标识 `BENCH_05_00005_006` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/006`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_006`、`metric_006_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_05_00005_007

Benchmark 标识 `BENCH_05_00005_007` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/007`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_007`、`metric_007_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_05_00005_008

Benchmark 标识 `BENCH_05_00005_008` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/008`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_008`、`metric_008_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_05_00005_009

Benchmark 标识 `BENCH_05_00005_009` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/009`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_009`、`metric_009_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_05_00005_010

Benchmark 标识 `BENCH_05_00005_010` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/010`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_010`、`metric_010_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_05_00005_011

Benchmark 标识 `BENCH_05_00005_011` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/011`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_011`、`metric_011_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_05_00005_012

Benchmark 标识 `BENCH_05_00005_012` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/012`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_012`、`metric_012_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_05_00005_013

Benchmark 标识 `BENCH_05_00005_013` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/013`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_013`、`metric_013_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_05_00005_014

Benchmark 标识 `BENCH_05_00005_014` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/014`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_014`、`metric_014_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_05_00005_015

Benchmark 标识 `BENCH_05_00005_015` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/015`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_015`、`metric_015_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_05_00005_016

Benchmark 标识 `BENCH_05_00005_016` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/016`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_016`、`metric_016_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_05_00005_017

Benchmark 标识 `BENCH_05_00005_017` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/017`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_017`、`metric_017_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_05_00005_018

Benchmark 标识 `BENCH_05_00005_018` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/018`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_018`、`metric_018_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_05_00005_019

Benchmark 标识 `BENCH_05_00005_019` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/019`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_019`、`metric_019_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_05_00005_020

Benchmark 标识 `BENCH_05_00005_020` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/020`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_020`、`metric_020_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_05_00005_021

Benchmark 标识 `BENCH_05_00005_021` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/021`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_021`、`metric_021_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_05_00005_022

Benchmark 标识 `BENCH_05_00005_022` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/022`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_022`、`metric_022_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_05_00005_023

Benchmark 标识 `BENCH_05_00005_023` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/023`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_023`、`metric_023_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_05_00005_024

Benchmark 标识 `BENCH_05_00005_024` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/024`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_024`、`metric_024_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_05_00005_025

Benchmark 标识 `BENCH_05_00005_025` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/025`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_025`、`metric_025_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_05_00005_026

Benchmark 标识 `BENCH_05_00005_026` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/026`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_026`、`metric_026_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_05_00005_027

Benchmark 标识 `BENCH_05_00005_027` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/027`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_027`、`metric_027_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_05_00005_028

Benchmark 标识 `BENCH_05_00005_028` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/028`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_028`、`metric_028_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_05_00005_029

Benchmark 标识 `BENCH_05_00005_029` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/029`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_029`、`metric_029_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_05_00005_030

Benchmark 标识 `BENCH_05_00005_030` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/030`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_030`、`metric_030_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_05_00005_031

Benchmark 标识 `BENCH_05_00005_031` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/031`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_031`、`metric_031_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_05_00005_032

Benchmark 标识 `BENCH_05_00005_032` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/032`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_032`、`metric_032_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_05_00005_033

Benchmark 标识 `BENCH_05_00005_033` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/033`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_033`、`metric_033_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_05_00005_034

Benchmark 标识 `BENCH_05_00005_034` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/034`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_034`、`metric_034_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_05_00005_035

Benchmark 标识 `BENCH_05_00005_035` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/035`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_035`、`metric_035_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_05_00005_036

Benchmark 标识 `BENCH_05_00005_036` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/036`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_036`、`metric_036_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_05_00005_037

Benchmark 标识 `BENCH_05_00005_037` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/037`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_037`、`metric_037_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_05_00005_038

Benchmark 标识 `BENCH_05_00005_038` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/038`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_038`、`metric_038_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_05_00005_039

Benchmark 标识 `BENCH_05_00005_039` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/039`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_039`、`metric_039_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_05_00005_040

Benchmark 标识 `BENCH_05_00005_040` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00005/040`。
核心表 `t_bench_bench_domain_05_05` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B005040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00005_040`、`metric_040_value`、`t_bench_bench_domain_05_05` 三个关键词是否同时出现在上下文。
