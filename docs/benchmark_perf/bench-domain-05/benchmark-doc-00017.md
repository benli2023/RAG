---
title: Benchmark Document 00017
domain: bench-domain-05
type: reference
summary: Synthetic benchmark document 00017 for bench-domain-05. Tokens include BENCH_05_00017_001 BENCH_05_00017_002 BENCH_05_00017_003 BENCH_05_00017_004 BENCH_05_00017_005.
keywords:
  - BENCH_05_00017_001
  - BENCH_05_00017_002
  - BENCH_05_00017_003
  - BENCH_05_00017_004
  - BENCH_05_00017_005
  - BENCH_05_00017_006
  - BENCH_05_00017_007
  - BENCH_05_00017_008
  - BENCH_05_00017_009
  - BENCH_05_00017_010
  - BENCH_05_00017_011
  - BENCH_05_00017_012
  - BENCH_05_00017_013
  - BENCH_05_00017_014
  - BENCH_05_00017_015
  - BENCH_05_00017_016
  - BENCH_05_00017_017
  - BENCH_05_00017_018
  - BENCH_05_00017_019
  - BENCH_05_00017_020
  - BENCH_05_00017_021
  - BENCH_05_00017_022
  - BENCH_05_00017_023
  - BENCH_05_00017_024
  - BENCH_05_00017_025
  - BENCH_05_00017_026
  - BENCH_05_00017_027
  - BENCH_05_00017_028
  - BENCH_05_00017_029
  - BENCH_05_00017_030
  - BENCH_05_00017_031
  - BENCH_05_00017_032
  - BENCH_05_00017_033
  - BENCH_05_00017_034
  - BENCH_05_00017_035
  - BENCH_05_00017_036
  - BENCH_05_00017_037
  - BENCH_05_00017_038
  - BENCH_05_00017_039
  - BENCH_05_00017_040
  - bench-domain-05
  - benchmark
related_domains:
  - bench-domain-05
  - global
acl: public
---

# Benchmark Document 00017

This generated document belongs to `bench-domain-05` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_05_00017_001

Benchmark 标识 `BENCH_05_00017_001` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/001`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_001`、`metric_001_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_05_00017_002

Benchmark 标识 `BENCH_05_00017_002` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/002`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_002`、`metric_002_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_05_00017_003

Benchmark 标识 `BENCH_05_00017_003` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/003`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_003`、`metric_003_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_05_00017_004

Benchmark 标识 `BENCH_05_00017_004` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/004`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_004`、`metric_004_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_05_00017_005

Benchmark 标识 `BENCH_05_00017_005` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/005`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_005`、`metric_005_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_05_00017_006

Benchmark 标识 `BENCH_05_00017_006` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/006`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_006`、`metric_006_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_05_00017_007

Benchmark 标识 `BENCH_05_00017_007` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/007`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_007`、`metric_007_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_05_00017_008

Benchmark 标识 `BENCH_05_00017_008` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/008`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_008`、`metric_008_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_05_00017_009

Benchmark 标识 `BENCH_05_00017_009` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/009`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_009`、`metric_009_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_05_00017_010

Benchmark 标识 `BENCH_05_00017_010` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/010`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_010`、`metric_010_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_05_00017_011

Benchmark 标识 `BENCH_05_00017_011` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/011`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_011`、`metric_011_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_05_00017_012

Benchmark 标识 `BENCH_05_00017_012` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/012`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_012`、`metric_012_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_05_00017_013

Benchmark 标识 `BENCH_05_00017_013` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/013`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_013`、`metric_013_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_05_00017_014

Benchmark 标识 `BENCH_05_00017_014` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/014`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_014`、`metric_014_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_05_00017_015

Benchmark 标识 `BENCH_05_00017_015` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/015`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_015`、`metric_015_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_05_00017_016

Benchmark 标识 `BENCH_05_00017_016` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/016`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_016`、`metric_016_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_05_00017_017

Benchmark 标识 `BENCH_05_00017_017` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/017`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_017`、`metric_017_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_05_00017_018

Benchmark 标识 `BENCH_05_00017_018` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/018`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_018`、`metric_018_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_05_00017_019

Benchmark 标识 `BENCH_05_00017_019` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/019`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_019`、`metric_019_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_05_00017_020

Benchmark 标识 `BENCH_05_00017_020` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/020`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_020`、`metric_020_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_05_00017_021

Benchmark 标识 `BENCH_05_00017_021` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/021`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_021`、`metric_021_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_05_00017_022

Benchmark 标识 `BENCH_05_00017_022` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/022`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_022`、`metric_022_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_05_00017_023

Benchmark 标识 `BENCH_05_00017_023` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/023`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_023`、`metric_023_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_05_00017_024

Benchmark 标识 `BENCH_05_00017_024` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/024`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_024`、`metric_024_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_05_00017_025

Benchmark 标识 `BENCH_05_00017_025` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/025`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_025`、`metric_025_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_05_00017_026

Benchmark 标识 `BENCH_05_00017_026` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/026`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_026`、`metric_026_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_05_00017_027

Benchmark 标识 `BENCH_05_00017_027` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/027`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_027`、`metric_027_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_05_00017_028

Benchmark 标识 `BENCH_05_00017_028` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/028`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_028`、`metric_028_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_05_00017_029

Benchmark 标识 `BENCH_05_00017_029` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/029`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_029`、`metric_029_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_05_00017_030

Benchmark 标识 `BENCH_05_00017_030` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/030`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_030`、`metric_030_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_05_00017_031

Benchmark 标识 `BENCH_05_00017_031` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/031`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_031`、`metric_031_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_05_00017_032

Benchmark 标识 `BENCH_05_00017_032` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/032`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_032`、`metric_032_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_05_00017_033

Benchmark 标识 `BENCH_05_00017_033` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/033`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_033`、`metric_033_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_05_00017_034

Benchmark 标识 `BENCH_05_00017_034` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/034`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_034`、`metric_034_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_05_00017_035

Benchmark 标识 `BENCH_05_00017_035` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/035`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_035`、`metric_035_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_05_00017_036

Benchmark 标识 `BENCH_05_00017_036` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/036`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_036`、`metric_036_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_05_00017_037

Benchmark 标识 `BENCH_05_00017_037` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/037`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_037`、`metric_037_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_05_00017_038

Benchmark 标识 `BENCH_05_00017_038` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/038`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_038`、`metric_038_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_05_00017_039

Benchmark 标识 `BENCH_05_00017_039` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/039`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_039`、`metric_039_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_05_00017_040

Benchmark 标识 `BENCH_05_00017_040` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00017/040`。
核心表 `t_bench_bench_domain_05_17` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B017040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00017_040`、`metric_040_value`、`t_bench_bench_domain_05_17` 三个关键词是否同时出现在上下文。
