---
title: Benchmark Document 00006
domain: bench-domain-06
type: reference
summary: Synthetic benchmark document 00006 for bench-domain-06. Tokens include BENCH_06_00006_001 BENCH_06_00006_002 BENCH_06_00006_003 BENCH_06_00006_004 BENCH_06_00006_005.
keywords:
  - BENCH_06_00006_001
  - BENCH_06_00006_002
  - BENCH_06_00006_003
  - BENCH_06_00006_004
  - BENCH_06_00006_005
  - BENCH_06_00006_006
  - BENCH_06_00006_007
  - BENCH_06_00006_008
  - BENCH_06_00006_009
  - BENCH_06_00006_010
  - BENCH_06_00006_011
  - BENCH_06_00006_012
  - BENCH_06_00006_013
  - BENCH_06_00006_014
  - BENCH_06_00006_015
  - BENCH_06_00006_016
  - BENCH_06_00006_017
  - BENCH_06_00006_018
  - BENCH_06_00006_019
  - BENCH_06_00006_020
  - BENCH_06_00006_021
  - BENCH_06_00006_022
  - BENCH_06_00006_023
  - BENCH_06_00006_024
  - BENCH_06_00006_025
  - BENCH_06_00006_026
  - BENCH_06_00006_027
  - BENCH_06_00006_028
  - BENCH_06_00006_029
  - BENCH_06_00006_030
  - BENCH_06_00006_031
  - BENCH_06_00006_032
  - BENCH_06_00006_033
  - BENCH_06_00006_034
  - BENCH_06_00006_035
  - BENCH_06_00006_036
  - BENCH_06_00006_037
  - BENCH_06_00006_038
  - BENCH_06_00006_039
  - BENCH_06_00006_040
  - bench-domain-06
  - benchmark
related_domains:
  - bench-domain-06
  - global
acl: public
---

# Benchmark Document 00006

This generated document belongs to `bench-domain-06` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_06_00006_001

Benchmark 标识 `BENCH_06_00006_001` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/001`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_001`、`metric_001_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_06_00006_002

Benchmark 标识 `BENCH_06_00006_002` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/002`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_002`、`metric_002_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_06_00006_003

Benchmark 标识 `BENCH_06_00006_003` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/003`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_003`、`metric_003_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_06_00006_004

Benchmark 标识 `BENCH_06_00006_004` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/004`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_004`、`metric_004_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_06_00006_005

Benchmark 标识 `BENCH_06_00006_005` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/005`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_005`、`metric_005_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_06_00006_006

Benchmark 标识 `BENCH_06_00006_006` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/006`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_006`、`metric_006_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_06_00006_007

Benchmark 标识 `BENCH_06_00006_007` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/007`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_007`、`metric_007_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_06_00006_008

Benchmark 标识 `BENCH_06_00006_008` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/008`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_008`、`metric_008_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_06_00006_009

Benchmark 标识 `BENCH_06_00006_009` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/009`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_009`、`metric_009_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_06_00006_010

Benchmark 标识 `BENCH_06_00006_010` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/010`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_010`、`metric_010_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_06_00006_011

Benchmark 标识 `BENCH_06_00006_011` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/011`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_011`、`metric_011_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_06_00006_012

Benchmark 标识 `BENCH_06_00006_012` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/012`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_012`、`metric_012_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_06_00006_013

Benchmark 标识 `BENCH_06_00006_013` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/013`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_013`、`metric_013_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_06_00006_014

Benchmark 标识 `BENCH_06_00006_014` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/014`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_014`、`metric_014_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_06_00006_015

Benchmark 标识 `BENCH_06_00006_015` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/015`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_015`、`metric_015_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_06_00006_016

Benchmark 标识 `BENCH_06_00006_016` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/016`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_016`、`metric_016_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_06_00006_017

Benchmark 标识 `BENCH_06_00006_017` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/017`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_017`、`metric_017_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_06_00006_018

Benchmark 标识 `BENCH_06_00006_018` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/018`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_018`、`metric_018_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_06_00006_019

Benchmark 标识 `BENCH_06_00006_019` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/019`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_019`、`metric_019_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_06_00006_020

Benchmark 标识 `BENCH_06_00006_020` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/020`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_020`、`metric_020_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_06_00006_021

Benchmark 标识 `BENCH_06_00006_021` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/021`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_021`、`metric_021_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_06_00006_022

Benchmark 标识 `BENCH_06_00006_022` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/022`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_022`、`metric_022_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_06_00006_023

Benchmark 标识 `BENCH_06_00006_023` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/023`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_023`、`metric_023_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_06_00006_024

Benchmark 标识 `BENCH_06_00006_024` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/024`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_024`、`metric_024_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_06_00006_025

Benchmark 标识 `BENCH_06_00006_025` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/025`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_025`、`metric_025_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_06_00006_026

Benchmark 标识 `BENCH_06_00006_026` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/026`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_026`、`metric_026_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_06_00006_027

Benchmark 标识 `BENCH_06_00006_027` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/027`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_027`、`metric_027_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_06_00006_028

Benchmark 标识 `BENCH_06_00006_028` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/028`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_028`、`metric_028_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_06_00006_029

Benchmark 标识 `BENCH_06_00006_029` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/029`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_029`、`metric_029_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_06_00006_030

Benchmark 标识 `BENCH_06_00006_030` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/030`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_030`、`metric_030_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_06_00006_031

Benchmark 标识 `BENCH_06_00006_031` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/031`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_031`、`metric_031_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_06_00006_032

Benchmark 标识 `BENCH_06_00006_032` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/032`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_032`、`metric_032_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_06_00006_033

Benchmark 标识 `BENCH_06_00006_033` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/033`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_033`、`metric_033_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_06_00006_034

Benchmark 标识 `BENCH_06_00006_034` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/034`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_034`、`metric_034_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_06_00006_035

Benchmark 标识 `BENCH_06_00006_035` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/035`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_035`、`metric_035_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_06_00006_036

Benchmark 标识 `BENCH_06_00006_036` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/036`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_036`、`metric_036_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_06_00006_037

Benchmark 标识 `BENCH_06_00006_037` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/037`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_037`、`metric_037_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_06_00006_038

Benchmark 标识 `BENCH_06_00006_038` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/038`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_038`、`metric_038_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_06_00006_039

Benchmark 标识 `BENCH_06_00006_039` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/039`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_039`、`metric_039_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_06_00006_040

Benchmark 标识 `BENCH_06_00006_040` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00006/040`。
核心表 `t_bench_bench_domain_06_06` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B006040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00006_040`、`metric_040_value`、`t_bench_bench_domain_06_06` 三个关键词是否同时出现在上下文。
