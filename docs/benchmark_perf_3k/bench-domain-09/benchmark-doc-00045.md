---
title: Benchmark Document 00045
domain: bench-domain-09
type: reference
summary: Synthetic benchmark document 00045 for bench-domain-09. Tokens include BENCH_09_00045_001 BENCH_09_00045_002 BENCH_09_00045_003 BENCH_09_00045_004 BENCH_09_00045_005.
keywords:
  - BENCH_09_00045_001
  - BENCH_09_00045_002
  - BENCH_09_00045_003
  - BENCH_09_00045_004
  - BENCH_09_00045_005
  - BENCH_09_00045_006
  - BENCH_09_00045_007
  - BENCH_09_00045_008
  - BENCH_09_00045_009
  - BENCH_09_00045_010
  - BENCH_09_00045_011
  - BENCH_09_00045_012
  - BENCH_09_00045_013
  - BENCH_09_00045_014
  - BENCH_09_00045_015
  - BENCH_09_00045_016
  - BENCH_09_00045_017
  - BENCH_09_00045_018
  - BENCH_09_00045_019
  - BENCH_09_00045_020
  - BENCH_09_00045_021
  - BENCH_09_00045_022
  - BENCH_09_00045_023
  - BENCH_09_00045_024
  - BENCH_09_00045_025
  - BENCH_09_00045_026
  - BENCH_09_00045_027
  - BENCH_09_00045_028
  - BENCH_09_00045_029
  - BENCH_09_00045_030
  - BENCH_09_00045_031
  - BENCH_09_00045_032
  - BENCH_09_00045_033
  - BENCH_09_00045_034
  - BENCH_09_00045_035
  - BENCH_09_00045_036
  - BENCH_09_00045_037
  - BENCH_09_00045_038
  - BENCH_09_00045_039
  - BENCH_09_00045_040
  - bench-domain-09
  - benchmark
related_domains:
  - bench-domain-09
  - global
acl: public
---

# Benchmark Document 00045

This generated document belongs to `bench-domain-09` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_09_00045_001

Benchmark 标识 `BENCH_09_00045_001` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/001`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_001`、`metric_001_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_09_00045_002

Benchmark 标识 `BENCH_09_00045_002` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/002`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_002`、`metric_002_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_09_00045_003

Benchmark 标识 `BENCH_09_00045_003` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/003`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_003`、`metric_003_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_09_00045_004

Benchmark 标识 `BENCH_09_00045_004` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/004`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_004`、`metric_004_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_09_00045_005

Benchmark 标识 `BENCH_09_00045_005` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/005`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_005`、`metric_005_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_09_00045_006

Benchmark 标识 `BENCH_09_00045_006` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/006`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_006`、`metric_006_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_09_00045_007

Benchmark 标识 `BENCH_09_00045_007` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/007`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_007`、`metric_007_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_09_00045_008

Benchmark 标识 `BENCH_09_00045_008` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/008`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_008`、`metric_008_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_09_00045_009

Benchmark 标识 `BENCH_09_00045_009` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/009`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_009`、`metric_009_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_09_00045_010

Benchmark 标识 `BENCH_09_00045_010` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/010`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_010`、`metric_010_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_09_00045_011

Benchmark 标识 `BENCH_09_00045_011` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/011`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_011`、`metric_011_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_09_00045_012

Benchmark 标识 `BENCH_09_00045_012` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/012`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_012`、`metric_012_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_09_00045_013

Benchmark 标识 `BENCH_09_00045_013` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/013`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_013`、`metric_013_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_09_00045_014

Benchmark 标识 `BENCH_09_00045_014` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/014`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_014`、`metric_014_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_09_00045_015

Benchmark 标识 `BENCH_09_00045_015` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/015`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_015`、`metric_015_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_09_00045_016

Benchmark 标识 `BENCH_09_00045_016` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/016`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_016`、`metric_016_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_09_00045_017

Benchmark 标识 `BENCH_09_00045_017` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/017`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_017`、`metric_017_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_09_00045_018

Benchmark 标识 `BENCH_09_00045_018` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/018`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_018`、`metric_018_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_09_00045_019

Benchmark 标识 `BENCH_09_00045_019` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/019`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_019`、`metric_019_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_09_00045_020

Benchmark 标识 `BENCH_09_00045_020` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/020`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_020`、`metric_020_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_09_00045_021

Benchmark 标识 `BENCH_09_00045_021` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/021`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_021`、`metric_021_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_09_00045_022

Benchmark 标识 `BENCH_09_00045_022` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/022`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_022`、`metric_022_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_09_00045_023

Benchmark 标识 `BENCH_09_00045_023` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/023`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_023`、`metric_023_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_09_00045_024

Benchmark 标识 `BENCH_09_00045_024` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/024`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_024`、`metric_024_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_09_00045_025

Benchmark 标识 `BENCH_09_00045_025` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/025`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_025`、`metric_025_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_09_00045_026

Benchmark 标识 `BENCH_09_00045_026` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/026`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_026`、`metric_026_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_09_00045_027

Benchmark 标识 `BENCH_09_00045_027` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/027`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_027`、`metric_027_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_09_00045_028

Benchmark 标识 `BENCH_09_00045_028` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/028`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_028`、`metric_028_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_09_00045_029

Benchmark 标识 `BENCH_09_00045_029` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/029`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_029`、`metric_029_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_09_00045_030

Benchmark 标识 `BENCH_09_00045_030` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/030`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_030`、`metric_030_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_09_00045_031

Benchmark 标识 `BENCH_09_00045_031` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/031`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_031`、`metric_031_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_09_00045_032

Benchmark 标识 `BENCH_09_00045_032` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/032`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_032`、`metric_032_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_09_00045_033

Benchmark 标识 `BENCH_09_00045_033` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/033`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_033`、`metric_033_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_09_00045_034

Benchmark 标识 `BENCH_09_00045_034` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/034`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_034`、`metric_034_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_09_00045_035

Benchmark 标识 `BENCH_09_00045_035` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/035`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_035`、`metric_035_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_09_00045_036

Benchmark 标识 `BENCH_09_00045_036` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/036`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_036`、`metric_036_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_09_00045_037

Benchmark 标识 `BENCH_09_00045_037` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/037`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_037`、`metric_037_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_09_00045_038

Benchmark 标识 `BENCH_09_00045_038` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/038`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_038`、`metric_038_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_09_00045_039

Benchmark 标识 `BENCH_09_00045_039` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/039`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_039`、`metric_039_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_09_00045_040

Benchmark 标识 `BENCH_09_00045_040` 属于 `bench-domain-09` 模块，接口路径是 `/api/benchmark/bench-domain-09/case/00045/040`。
核心表 `t_bench_bench_domain_09_45` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B045040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_09_00045_040`、`metric_040_value`、`t_bench_bench_domain_09_45` 三个关键词是否同时出现在上下文。
