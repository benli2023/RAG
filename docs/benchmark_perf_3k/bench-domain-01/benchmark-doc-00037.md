---
title: Benchmark Document 00037
domain: bench-domain-01
type: reference
summary: Synthetic benchmark document 00037 for bench-domain-01. Tokens include BENCH_01_00037_001 BENCH_01_00037_002 BENCH_01_00037_003 BENCH_01_00037_004 BENCH_01_00037_005.
keywords:
  - BENCH_01_00037_001
  - BENCH_01_00037_002
  - BENCH_01_00037_003
  - BENCH_01_00037_004
  - BENCH_01_00037_005
  - BENCH_01_00037_006
  - BENCH_01_00037_007
  - BENCH_01_00037_008
  - BENCH_01_00037_009
  - BENCH_01_00037_010
  - BENCH_01_00037_011
  - BENCH_01_00037_012
  - BENCH_01_00037_013
  - BENCH_01_00037_014
  - BENCH_01_00037_015
  - BENCH_01_00037_016
  - BENCH_01_00037_017
  - BENCH_01_00037_018
  - BENCH_01_00037_019
  - BENCH_01_00037_020
  - BENCH_01_00037_021
  - BENCH_01_00037_022
  - BENCH_01_00037_023
  - BENCH_01_00037_024
  - BENCH_01_00037_025
  - BENCH_01_00037_026
  - BENCH_01_00037_027
  - BENCH_01_00037_028
  - BENCH_01_00037_029
  - BENCH_01_00037_030
  - BENCH_01_00037_031
  - BENCH_01_00037_032
  - BENCH_01_00037_033
  - BENCH_01_00037_034
  - BENCH_01_00037_035
  - BENCH_01_00037_036
  - BENCH_01_00037_037
  - BENCH_01_00037_038
  - BENCH_01_00037_039
  - BENCH_01_00037_040
  - bench-domain-01
  - benchmark
related_domains:
  - bench-domain-01
  - global
acl: public
---

# Benchmark Document 00037

This generated document belongs to `bench-domain-01` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_01_00037_001

Benchmark 标识 `BENCH_01_00037_001` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/001`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_001`、`metric_001_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_01_00037_002

Benchmark 标识 `BENCH_01_00037_002` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/002`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_002`、`metric_002_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_01_00037_003

Benchmark 标识 `BENCH_01_00037_003` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/003`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_003`、`metric_003_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_01_00037_004

Benchmark 标识 `BENCH_01_00037_004` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/004`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_004`、`metric_004_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_01_00037_005

Benchmark 标识 `BENCH_01_00037_005` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/005`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_005`、`metric_005_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_01_00037_006

Benchmark 标识 `BENCH_01_00037_006` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/006`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_006`、`metric_006_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_01_00037_007

Benchmark 标识 `BENCH_01_00037_007` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/007`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_007`、`metric_007_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_01_00037_008

Benchmark 标识 `BENCH_01_00037_008` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/008`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_008`、`metric_008_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_01_00037_009

Benchmark 标识 `BENCH_01_00037_009` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/009`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_009`、`metric_009_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_01_00037_010

Benchmark 标识 `BENCH_01_00037_010` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/010`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_010`、`metric_010_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_01_00037_011

Benchmark 标识 `BENCH_01_00037_011` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/011`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_011`、`metric_011_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_01_00037_012

Benchmark 标识 `BENCH_01_00037_012` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/012`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_012`、`metric_012_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_01_00037_013

Benchmark 标识 `BENCH_01_00037_013` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/013`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_013`、`metric_013_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_01_00037_014

Benchmark 标识 `BENCH_01_00037_014` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/014`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_014`、`metric_014_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_01_00037_015

Benchmark 标识 `BENCH_01_00037_015` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/015`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_015`、`metric_015_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_01_00037_016

Benchmark 标识 `BENCH_01_00037_016` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/016`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_016`、`metric_016_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_01_00037_017

Benchmark 标识 `BENCH_01_00037_017` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/017`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_017`、`metric_017_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_01_00037_018

Benchmark 标识 `BENCH_01_00037_018` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/018`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_018`、`metric_018_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_01_00037_019

Benchmark 标识 `BENCH_01_00037_019` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/019`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_019`、`metric_019_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_01_00037_020

Benchmark 标识 `BENCH_01_00037_020` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/020`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_020`、`metric_020_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_01_00037_021

Benchmark 标识 `BENCH_01_00037_021` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/021`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_021`、`metric_021_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_01_00037_022

Benchmark 标识 `BENCH_01_00037_022` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/022`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_022`、`metric_022_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_01_00037_023

Benchmark 标识 `BENCH_01_00037_023` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/023`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_023`、`metric_023_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_01_00037_024

Benchmark 标识 `BENCH_01_00037_024` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/024`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_024`、`metric_024_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_01_00037_025

Benchmark 标识 `BENCH_01_00037_025` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/025`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_025`、`metric_025_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_01_00037_026

Benchmark 标识 `BENCH_01_00037_026` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/026`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_026`、`metric_026_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_01_00037_027

Benchmark 标识 `BENCH_01_00037_027` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/027`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_027`、`metric_027_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_01_00037_028

Benchmark 标识 `BENCH_01_00037_028` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/028`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_028`、`metric_028_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_01_00037_029

Benchmark 标识 `BENCH_01_00037_029` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/029`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_029`、`metric_029_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_01_00037_030

Benchmark 标识 `BENCH_01_00037_030` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/030`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_030`、`metric_030_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_01_00037_031

Benchmark 标识 `BENCH_01_00037_031` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/031`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_031`、`metric_031_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_01_00037_032

Benchmark 标识 `BENCH_01_00037_032` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/032`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_032`、`metric_032_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_01_00037_033

Benchmark 标识 `BENCH_01_00037_033` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/033`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_033`、`metric_033_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_01_00037_034

Benchmark 标识 `BENCH_01_00037_034` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/034`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_034`、`metric_034_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_01_00037_035

Benchmark 标识 `BENCH_01_00037_035` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/035`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_035`、`metric_035_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_01_00037_036

Benchmark 标识 `BENCH_01_00037_036` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/036`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_036`、`metric_036_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_01_00037_037

Benchmark 标识 `BENCH_01_00037_037` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/037`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_037`、`metric_037_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_01_00037_038

Benchmark 标识 `BENCH_01_00037_038` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/038`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_038`、`metric_038_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_01_00037_039

Benchmark 标识 `BENCH_01_00037_039` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/039`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_039`、`metric_039_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_01_00037_040

Benchmark 标识 `BENCH_01_00037_040` 属于 `bench-domain-01` 模块，接口路径是 `/api/benchmark/bench-domain-01/case/00037/040`。
核心表 `t_bench_bench_domain_01_37` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B037040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_01_00037_040`、`metric_040_value`、`t_bench_bench_domain_01_37` 三个关键词是否同时出现在上下文。
