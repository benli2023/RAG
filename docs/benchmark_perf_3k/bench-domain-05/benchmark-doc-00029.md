---
title: Benchmark Document 00029
domain: bench-domain-05
type: reference
summary: Synthetic benchmark document 00029 for bench-domain-05. Tokens include BENCH_05_00029_001 BENCH_05_00029_002 BENCH_05_00029_003 BENCH_05_00029_004 BENCH_05_00029_005.
keywords:
  - BENCH_05_00029_001
  - BENCH_05_00029_002
  - BENCH_05_00029_003
  - BENCH_05_00029_004
  - BENCH_05_00029_005
  - BENCH_05_00029_006
  - BENCH_05_00029_007
  - BENCH_05_00029_008
  - BENCH_05_00029_009
  - BENCH_05_00029_010
  - BENCH_05_00029_011
  - BENCH_05_00029_012
  - BENCH_05_00029_013
  - BENCH_05_00029_014
  - BENCH_05_00029_015
  - BENCH_05_00029_016
  - BENCH_05_00029_017
  - BENCH_05_00029_018
  - BENCH_05_00029_019
  - BENCH_05_00029_020
  - BENCH_05_00029_021
  - BENCH_05_00029_022
  - BENCH_05_00029_023
  - BENCH_05_00029_024
  - BENCH_05_00029_025
  - BENCH_05_00029_026
  - BENCH_05_00029_027
  - BENCH_05_00029_028
  - BENCH_05_00029_029
  - BENCH_05_00029_030
  - BENCH_05_00029_031
  - BENCH_05_00029_032
  - BENCH_05_00029_033
  - BENCH_05_00029_034
  - BENCH_05_00029_035
  - BENCH_05_00029_036
  - BENCH_05_00029_037
  - BENCH_05_00029_038
  - BENCH_05_00029_039
  - BENCH_05_00029_040
  - bench-domain-05
  - benchmark
related_domains:
  - bench-domain-05
  - global
acl: public
---

# Benchmark Document 00029

This generated document belongs to `bench-domain-05` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_05_00029_001

Benchmark 标识 `BENCH_05_00029_001` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/001`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_001`、`metric_001_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_05_00029_002

Benchmark 标识 `BENCH_05_00029_002` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/002`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_002`、`metric_002_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_05_00029_003

Benchmark 标识 `BENCH_05_00029_003` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/003`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_003`、`metric_003_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_05_00029_004

Benchmark 标识 `BENCH_05_00029_004` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/004`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_004`、`metric_004_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_05_00029_005

Benchmark 标识 `BENCH_05_00029_005` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/005`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_005`、`metric_005_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_05_00029_006

Benchmark 标识 `BENCH_05_00029_006` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/006`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_006`、`metric_006_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_05_00029_007

Benchmark 标识 `BENCH_05_00029_007` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/007`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_007`、`metric_007_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_05_00029_008

Benchmark 标识 `BENCH_05_00029_008` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/008`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_008`、`metric_008_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_05_00029_009

Benchmark 标识 `BENCH_05_00029_009` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/009`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_009`、`metric_009_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_05_00029_010

Benchmark 标识 `BENCH_05_00029_010` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/010`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_010`、`metric_010_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_05_00029_011

Benchmark 标识 `BENCH_05_00029_011` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/011`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_011`、`metric_011_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_05_00029_012

Benchmark 标识 `BENCH_05_00029_012` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/012`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_012`、`metric_012_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_05_00029_013

Benchmark 标识 `BENCH_05_00029_013` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/013`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_013`、`metric_013_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_05_00029_014

Benchmark 标识 `BENCH_05_00029_014` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/014`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_014`、`metric_014_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_05_00029_015

Benchmark 标识 `BENCH_05_00029_015` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/015`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_015`、`metric_015_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_05_00029_016

Benchmark 标识 `BENCH_05_00029_016` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/016`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_016`、`metric_016_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_05_00029_017

Benchmark 标识 `BENCH_05_00029_017` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/017`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_017`、`metric_017_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_05_00029_018

Benchmark 标识 `BENCH_05_00029_018` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/018`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_018`、`metric_018_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_05_00029_019

Benchmark 标识 `BENCH_05_00029_019` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/019`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_019`、`metric_019_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_05_00029_020

Benchmark 标识 `BENCH_05_00029_020` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/020`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_020`、`metric_020_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_05_00029_021

Benchmark 标识 `BENCH_05_00029_021` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/021`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_021`、`metric_021_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_05_00029_022

Benchmark 标识 `BENCH_05_00029_022` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/022`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_022`、`metric_022_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_05_00029_023

Benchmark 标识 `BENCH_05_00029_023` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/023`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_023`、`metric_023_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_05_00029_024

Benchmark 标识 `BENCH_05_00029_024` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/024`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_024`、`metric_024_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_05_00029_025

Benchmark 标识 `BENCH_05_00029_025` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/025`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_025`、`metric_025_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_05_00029_026

Benchmark 标识 `BENCH_05_00029_026` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/026`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_026`、`metric_026_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_05_00029_027

Benchmark 标识 `BENCH_05_00029_027` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/027`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_027`、`metric_027_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_05_00029_028

Benchmark 标识 `BENCH_05_00029_028` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/028`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_028`、`metric_028_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_05_00029_029

Benchmark 标识 `BENCH_05_00029_029` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/029`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_029`、`metric_029_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_05_00029_030

Benchmark 标识 `BENCH_05_00029_030` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/030`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_030`、`metric_030_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_05_00029_031

Benchmark 标识 `BENCH_05_00029_031` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/031`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_031`、`metric_031_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_05_00029_032

Benchmark 标识 `BENCH_05_00029_032` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/032`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_032`、`metric_032_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_05_00029_033

Benchmark 标识 `BENCH_05_00029_033` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/033`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_033`、`metric_033_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_05_00029_034

Benchmark 标识 `BENCH_05_00029_034` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/034`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_034`、`metric_034_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_05_00029_035

Benchmark 标识 `BENCH_05_00029_035` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/035`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_035`、`metric_035_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_05_00029_036

Benchmark 标识 `BENCH_05_00029_036` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/036`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_036`、`metric_036_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_05_00029_037

Benchmark 标识 `BENCH_05_00029_037` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/037`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_037`、`metric_037_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_05_00029_038

Benchmark 标识 `BENCH_05_00029_038` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/038`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_038`、`metric_038_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_05_00029_039

Benchmark 标识 `BENCH_05_00029_039` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/039`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_039`、`metric_039_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_05_00029_040

Benchmark 标识 `BENCH_05_00029_040` 属于 `bench-domain-05` 模块，接口路径是 `/api/benchmark/bench-domain-05/case/00029/040`。
核心表 `t_bench_bench_domain_05_29` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B029040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_05_00029_040`、`metric_040_value`、`t_bench_bench_domain_05_29` 三个关键词是否同时出现在上下文。
