---
title: Benchmark Document 00040
domain: bench-domain-04
type: reference
summary: Synthetic benchmark document 00040 for bench-domain-04. Tokens include BENCH_04_00040_001 BENCH_04_00040_002 BENCH_04_00040_003 BENCH_04_00040_004 BENCH_04_00040_005.
keywords:
  - BENCH_04_00040_001
  - BENCH_04_00040_002
  - BENCH_04_00040_003
  - BENCH_04_00040_004
  - BENCH_04_00040_005
  - BENCH_04_00040_006
  - BENCH_04_00040_007
  - BENCH_04_00040_008
  - BENCH_04_00040_009
  - BENCH_04_00040_010
  - BENCH_04_00040_011
  - BENCH_04_00040_012
  - BENCH_04_00040_013
  - BENCH_04_00040_014
  - BENCH_04_00040_015
  - BENCH_04_00040_016
  - BENCH_04_00040_017
  - BENCH_04_00040_018
  - BENCH_04_00040_019
  - BENCH_04_00040_020
  - BENCH_04_00040_021
  - BENCH_04_00040_022
  - BENCH_04_00040_023
  - BENCH_04_00040_024
  - BENCH_04_00040_025
  - BENCH_04_00040_026
  - BENCH_04_00040_027
  - BENCH_04_00040_028
  - BENCH_04_00040_029
  - BENCH_04_00040_030
  - BENCH_04_00040_031
  - BENCH_04_00040_032
  - BENCH_04_00040_033
  - BENCH_04_00040_034
  - BENCH_04_00040_035
  - BENCH_04_00040_036
  - BENCH_04_00040_037
  - BENCH_04_00040_038
  - BENCH_04_00040_039
  - BENCH_04_00040_040
  - bench-domain-04
  - benchmark
related_domains:
  - bench-domain-04
  - global
acl: public
---

# Benchmark Document 00040

This generated document belongs to `bench-domain-04` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_04_00040_001

Benchmark 标识 `BENCH_04_00040_001` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/001`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_001`、`metric_001_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_04_00040_002

Benchmark 标识 `BENCH_04_00040_002` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/002`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_002`、`metric_002_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_04_00040_003

Benchmark 标识 `BENCH_04_00040_003` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/003`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_003`、`metric_003_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_04_00040_004

Benchmark 标识 `BENCH_04_00040_004` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/004`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_004`、`metric_004_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_04_00040_005

Benchmark 标识 `BENCH_04_00040_005` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/005`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_005`、`metric_005_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_04_00040_006

Benchmark 标识 `BENCH_04_00040_006` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/006`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_006`、`metric_006_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_04_00040_007

Benchmark 标识 `BENCH_04_00040_007` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/007`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_007`、`metric_007_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_04_00040_008

Benchmark 标识 `BENCH_04_00040_008` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/008`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_008`、`metric_008_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_04_00040_009

Benchmark 标识 `BENCH_04_00040_009` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/009`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_009`、`metric_009_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_04_00040_010

Benchmark 标识 `BENCH_04_00040_010` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/010`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_010`、`metric_010_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_04_00040_011

Benchmark 标识 `BENCH_04_00040_011` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/011`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_011`、`metric_011_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_04_00040_012

Benchmark 标识 `BENCH_04_00040_012` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/012`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_012`、`metric_012_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_04_00040_013

Benchmark 标识 `BENCH_04_00040_013` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/013`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_013`、`metric_013_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_04_00040_014

Benchmark 标识 `BENCH_04_00040_014` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/014`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_014`、`metric_014_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_04_00040_015

Benchmark 标识 `BENCH_04_00040_015` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/015`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_015`、`metric_015_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_04_00040_016

Benchmark 标识 `BENCH_04_00040_016` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/016`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_016`、`metric_016_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_04_00040_017

Benchmark 标识 `BENCH_04_00040_017` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/017`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_017`、`metric_017_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_04_00040_018

Benchmark 标识 `BENCH_04_00040_018` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/018`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_018`、`metric_018_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_04_00040_019

Benchmark 标识 `BENCH_04_00040_019` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/019`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_019`、`metric_019_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_04_00040_020

Benchmark 标识 `BENCH_04_00040_020` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/020`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_020`、`metric_020_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_04_00040_021

Benchmark 标识 `BENCH_04_00040_021` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/021`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_021`、`metric_021_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_04_00040_022

Benchmark 标识 `BENCH_04_00040_022` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/022`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_022`、`metric_022_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_04_00040_023

Benchmark 标识 `BENCH_04_00040_023` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/023`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_023`、`metric_023_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_04_00040_024

Benchmark 标识 `BENCH_04_00040_024` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/024`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_024`、`metric_024_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_04_00040_025

Benchmark 标识 `BENCH_04_00040_025` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/025`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_025`、`metric_025_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_04_00040_026

Benchmark 标识 `BENCH_04_00040_026` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/026`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_026`、`metric_026_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_04_00040_027

Benchmark 标识 `BENCH_04_00040_027` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/027`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_027`、`metric_027_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_04_00040_028

Benchmark 标识 `BENCH_04_00040_028` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/028`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_028`、`metric_028_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_04_00040_029

Benchmark 标识 `BENCH_04_00040_029` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/029`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_029`、`metric_029_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_04_00040_030

Benchmark 标识 `BENCH_04_00040_030` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/030`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_030`、`metric_030_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_04_00040_031

Benchmark 标识 `BENCH_04_00040_031` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/031`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_031`、`metric_031_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_04_00040_032

Benchmark 标识 `BENCH_04_00040_032` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/032`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_032`、`metric_032_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_04_00040_033

Benchmark 标识 `BENCH_04_00040_033` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/033`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_033`、`metric_033_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_04_00040_034

Benchmark 标识 `BENCH_04_00040_034` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/034`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_034`、`metric_034_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_04_00040_035

Benchmark 标识 `BENCH_04_00040_035` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/035`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_035`、`metric_035_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_04_00040_036

Benchmark 标识 `BENCH_04_00040_036` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/036`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_036`、`metric_036_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_04_00040_037

Benchmark 标识 `BENCH_04_00040_037` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/037`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_037`、`metric_037_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_04_00040_038

Benchmark 标识 `BENCH_04_00040_038` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/038`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_038`、`metric_038_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_04_00040_039

Benchmark 标识 `BENCH_04_00040_039` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/039`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_039`、`metric_039_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_04_00040_040

Benchmark 标识 `BENCH_04_00040_040` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00040/040`。
核心表 `t_bench_bench_domain_04_40` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B040040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00040_040`、`metric_040_value`、`t_bench_bench_domain_04_40` 三个关键词是否同时出现在上下文。
