---
title: Benchmark Document 00028
domain: bench-domain-04
type: reference
summary: Synthetic benchmark document 00028 for bench-domain-04. Tokens include BENCH_04_00028_001 BENCH_04_00028_002 BENCH_04_00028_003 BENCH_04_00028_004 BENCH_04_00028_005.
keywords:
  - BENCH_04_00028_001
  - BENCH_04_00028_002
  - BENCH_04_00028_003
  - BENCH_04_00028_004
  - BENCH_04_00028_005
  - BENCH_04_00028_006
  - BENCH_04_00028_007
  - BENCH_04_00028_008
  - BENCH_04_00028_009
  - BENCH_04_00028_010
  - BENCH_04_00028_011
  - BENCH_04_00028_012
  - BENCH_04_00028_013
  - BENCH_04_00028_014
  - BENCH_04_00028_015
  - BENCH_04_00028_016
  - BENCH_04_00028_017
  - BENCH_04_00028_018
  - BENCH_04_00028_019
  - BENCH_04_00028_020
  - BENCH_04_00028_021
  - BENCH_04_00028_022
  - BENCH_04_00028_023
  - BENCH_04_00028_024
  - BENCH_04_00028_025
  - BENCH_04_00028_026
  - BENCH_04_00028_027
  - BENCH_04_00028_028
  - BENCH_04_00028_029
  - BENCH_04_00028_030
  - BENCH_04_00028_031
  - BENCH_04_00028_032
  - BENCH_04_00028_033
  - BENCH_04_00028_034
  - BENCH_04_00028_035
  - BENCH_04_00028_036
  - BENCH_04_00028_037
  - BENCH_04_00028_038
  - BENCH_04_00028_039
  - BENCH_04_00028_040
  - bench-domain-04
  - benchmark
related_domains:
  - bench-domain-04
  - global
acl: public
---

# Benchmark Document 00028

This generated document belongs to `bench-domain-04` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_04_00028_001

Benchmark 标识 `BENCH_04_00028_001` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/001`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_001`、`metric_001_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_04_00028_002

Benchmark 标识 `BENCH_04_00028_002` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/002`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_002`、`metric_002_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_04_00028_003

Benchmark 标识 `BENCH_04_00028_003` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/003`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_003`、`metric_003_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_04_00028_004

Benchmark 标识 `BENCH_04_00028_004` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/004`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_004`、`metric_004_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_04_00028_005

Benchmark 标识 `BENCH_04_00028_005` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/005`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_005`、`metric_005_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_04_00028_006

Benchmark 标识 `BENCH_04_00028_006` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/006`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_006`、`metric_006_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_04_00028_007

Benchmark 标识 `BENCH_04_00028_007` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/007`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_007`、`metric_007_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_04_00028_008

Benchmark 标识 `BENCH_04_00028_008` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/008`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_008`、`metric_008_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_04_00028_009

Benchmark 标识 `BENCH_04_00028_009` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/009`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_009`、`metric_009_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_04_00028_010

Benchmark 标识 `BENCH_04_00028_010` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/010`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_010`、`metric_010_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_04_00028_011

Benchmark 标识 `BENCH_04_00028_011` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/011`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_011`、`metric_011_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_04_00028_012

Benchmark 标识 `BENCH_04_00028_012` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/012`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_012`、`metric_012_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_04_00028_013

Benchmark 标识 `BENCH_04_00028_013` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/013`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_013`、`metric_013_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_04_00028_014

Benchmark 标识 `BENCH_04_00028_014` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/014`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_014`、`metric_014_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_04_00028_015

Benchmark 标识 `BENCH_04_00028_015` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/015`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_015`、`metric_015_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_04_00028_016

Benchmark 标识 `BENCH_04_00028_016` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/016`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_016`、`metric_016_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_04_00028_017

Benchmark 标识 `BENCH_04_00028_017` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/017`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_017`、`metric_017_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_04_00028_018

Benchmark 标识 `BENCH_04_00028_018` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/018`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_018`、`metric_018_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_04_00028_019

Benchmark 标识 `BENCH_04_00028_019` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/019`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_019`、`metric_019_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_04_00028_020

Benchmark 标识 `BENCH_04_00028_020` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/020`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_020`、`metric_020_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_04_00028_021

Benchmark 标识 `BENCH_04_00028_021` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/021`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_021`、`metric_021_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_04_00028_022

Benchmark 标识 `BENCH_04_00028_022` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/022`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_022`、`metric_022_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_04_00028_023

Benchmark 标识 `BENCH_04_00028_023` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/023`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_023`、`metric_023_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_04_00028_024

Benchmark 标识 `BENCH_04_00028_024` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/024`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_024`、`metric_024_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_04_00028_025

Benchmark 标识 `BENCH_04_00028_025` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/025`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_025`、`metric_025_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_04_00028_026

Benchmark 标识 `BENCH_04_00028_026` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/026`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_026`、`metric_026_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_04_00028_027

Benchmark 标识 `BENCH_04_00028_027` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/027`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_027`、`metric_027_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_04_00028_028

Benchmark 标识 `BENCH_04_00028_028` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/028`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_028`、`metric_028_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_04_00028_029

Benchmark 标识 `BENCH_04_00028_029` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/029`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_029`、`metric_029_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_04_00028_030

Benchmark 标识 `BENCH_04_00028_030` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/030`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_030`、`metric_030_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_04_00028_031

Benchmark 标识 `BENCH_04_00028_031` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/031`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_031`、`metric_031_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_04_00028_032

Benchmark 标识 `BENCH_04_00028_032` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/032`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_032`、`metric_032_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_04_00028_033

Benchmark 标识 `BENCH_04_00028_033` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/033`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_033`、`metric_033_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_04_00028_034

Benchmark 标识 `BENCH_04_00028_034` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/034`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_034`、`metric_034_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_04_00028_035

Benchmark 标识 `BENCH_04_00028_035` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/035`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_035`、`metric_035_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_04_00028_036

Benchmark 标识 `BENCH_04_00028_036` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/036`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_036`、`metric_036_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_04_00028_037

Benchmark 标识 `BENCH_04_00028_037` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/037`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_037`、`metric_037_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_04_00028_038

Benchmark 标识 `BENCH_04_00028_038` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/038`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_038`、`metric_038_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_04_00028_039

Benchmark 标识 `BENCH_04_00028_039` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/039`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_039`、`metric_039_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_04_00028_040

Benchmark 标识 `BENCH_04_00028_040` 属于 `bench-domain-04` 模块，接口路径是 `/api/benchmark/bench-domain-04/case/00028/040`。
核心表 `t_bench_bench_domain_04_28` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B028040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_04_00028_040`、`metric_040_value`、`t_bench_bench_domain_04_28` 三个关键词是否同时出现在上下文。
