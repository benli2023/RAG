---
title: Benchmark Document 00060
domain: bench-domain-00
type: reference
summary: Synthetic benchmark document 00060 for bench-domain-00. Tokens include BENCH_00_00060_001 BENCH_00_00060_002 BENCH_00_00060_003 BENCH_00_00060_004 BENCH_00_00060_005.
keywords:
  - BENCH_00_00060_001
  - BENCH_00_00060_002
  - BENCH_00_00060_003
  - BENCH_00_00060_004
  - BENCH_00_00060_005
  - BENCH_00_00060_006
  - BENCH_00_00060_007
  - BENCH_00_00060_008
  - BENCH_00_00060_009
  - BENCH_00_00060_010
  - BENCH_00_00060_011
  - BENCH_00_00060_012
  - BENCH_00_00060_013
  - BENCH_00_00060_014
  - BENCH_00_00060_015
  - BENCH_00_00060_016
  - BENCH_00_00060_017
  - BENCH_00_00060_018
  - BENCH_00_00060_019
  - BENCH_00_00060_020
  - BENCH_00_00060_021
  - BENCH_00_00060_022
  - BENCH_00_00060_023
  - BENCH_00_00060_024
  - BENCH_00_00060_025
  - BENCH_00_00060_026
  - BENCH_00_00060_027
  - BENCH_00_00060_028
  - BENCH_00_00060_029
  - BENCH_00_00060_030
  - BENCH_00_00060_031
  - BENCH_00_00060_032
  - BENCH_00_00060_033
  - BENCH_00_00060_034
  - BENCH_00_00060_035
  - BENCH_00_00060_036
  - BENCH_00_00060_037
  - BENCH_00_00060_038
  - BENCH_00_00060_039
  - BENCH_00_00060_040
  - bench-domain-00
  - benchmark
related_domains:
  - bench-domain-00
  - global
acl: public
---

# Benchmark Document 00060

This generated document belongs to `bench-domain-00` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_00_00060_001

Benchmark 标识 `BENCH_00_00060_001` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/001`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_001`、`metric_001_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_00_00060_002

Benchmark 标识 `BENCH_00_00060_002` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/002`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_002`、`metric_002_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_00_00060_003

Benchmark 标识 `BENCH_00_00060_003` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/003`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_003`、`metric_003_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_00_00060_004

Benchmark 标识 `BENCH_00_00060_004` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/004`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_004`、`metric_004_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_00_00060_005

Benchmark 标识 `BENCH_00_00060_005` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/005`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_005`、`metric_005_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_00_00060_006

Benchmark 标识 `BENCH_00_00060_006` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/006`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_006`、`metric_006_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_00_00060_007

Benchmark 标识 `BENCH_00_00060_007` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/007`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_007`、`metric_007_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_00_00060_008

Benchmark 标识 `BENCH_00_00060_008` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/008`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_008`、`metric_008_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_00_00060_009

Benchmark 标识 `BENCH_00_00060_009` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/009`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_009`、`metric_009_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_00_00060_010

Benchmark 标识 `BENCH_00_00060_010` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/010`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_010`、`metric_010_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_00_00060_011

Benchmark 标识 `BENCH_00_00060_011` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/011`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_011`、`metric_011_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_00_00060_012

Benchmark 标识 `BENCH_00_00060_012` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/012`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_012`、`metric_012_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_00_00060_013

Benchmark 标识 `BENCH_00_00060_013` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/013`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_013`、`metric_013_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_00_00060_014

Benchmark 标识 `BENCH_00_00060_014` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/014`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_014`、`metric_014_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_00_00060_015

Benchmark 标识 `BENCH_00_00060_015` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/015`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_015`、`metric_015_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_00_00060_016

Benchmark 标识 `BENCH_00_00060_016` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/016`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_016`、`metric_016_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_00_00060_017

Benchmark 标识 `BENCH_00_00060_017` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/017`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_017`、`metric_017_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_00_00060_018

Benchmark 标识 `BENCH_00_00060_018` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/018`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_018`、`metric_018_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_00_00060_019

Benchmark 标识 `BENCH_00_00060_019` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/019`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_019`、`metric_019_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_00_00060_020

Benchmark 标识 `BENCH_00_00060_020` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/020`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_020`、`metric_020_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_00_00060_021

Benchmark 标识 `BENCH_00_00060_021` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/021`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_021`、`metric_021_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_00_00060_022

Benchmark 标识 `BENCH_00_00060_022` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/022`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_022`、`metric_022_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_00_00060_023

Benchmark 标识 `BENCH_00_00060_023` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/023`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_023`、`metric_023_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_00_00060_024

Benchmark 标识 `BENCH_00_00060_024` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/024`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_024`、`metric_024_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_00_00060_025

Benchmark 标识 `BENCH_00_00060_025` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/025`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_025`、`metric_025_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_00_00060_026

Benchmark 标识 `BENCH_00_00060_026` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/026`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_026`、`metric_026_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_00_00060_027

Benchmark 标识 `BENCH_00_00060_027` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/027`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_027`、`metric_027_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_00_00060_028

Benchmark 标识 `BENCH_00_00060_028` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/028`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_028`、`metric_028_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_00_00060_029

Benchmark 标识 `BENCH_00_00060_029` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/029`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_029`、`metric_029_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_00_00060_030

Benchmark 标识 `BENCH_00_00060_030` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/030`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_030`、`metric_030_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_00_00060_031

Benchmark 标识 `BENCH_00_00060_031` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/031`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_031`、`metric_031_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_00_00060_032

Benchmark 标识 `BENCH_00_00060_032` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/032`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_032`、`metric_032_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_00_00060_033

Benchmark 标识 `BENCH_00_00060_033` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/033`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_033`、`metric_033_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_00_00060_034

Benchmark 标识 `BENCH_00_00060_034` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/034`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_034`、`metric_034_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_00_00060_035

Benchmark 标识 `BENCH_00_00060_035` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/035`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_035`、`metric_035_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_00_00060_036

Benchmark 标识 `BENCH_00_00060_036` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/036`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_036`、`metric_036_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_00_00060_037

Benchmark 标识 `BENCH_00_00060_037` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/037`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_037`、`metric_037_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_00_00060_038

Benchmark 标识 `BENCH_00_00060_038` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/038`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_038`、`metric_038_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_00_00060_039

Benchmark 标识 `BENCH_00_00060_039` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/039`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_039`、`metric_039_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_00_00060_040

Benchmark 标识 `BENCH_00_00060_040` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00060/040`。
核心表 `t_bench_bench_domain_00_60` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B060040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00060_040`、`metric_040_value`、`t_bench_bench_domain_00_60` 三个关键词是否同时出现在上下文。
