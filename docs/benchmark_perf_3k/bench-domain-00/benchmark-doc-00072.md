---
title: Benchmark Document 00072
domain: bench-domain-00
type: reference
summary: Synthetic benchmark document 00072 for bench-domain-00. Tokens include BENCH_00_00072_001 BENCH_00_00072_002 BENCH_00_00072_003 BENCH_00_00072_004 BENCH_00_00072_005.
keywords:
  - BENCH_00_00072_001
  - BENCH_00_00072_002
  - BENCH_00_00072_003
  - BENCH_00_00072_004
  - BENCH_00_00072_005
  - BENCH_00_00072_006
  - BENCH_00_00072_007
  - BENCH_00_00072_008
  - BENCH_00_00072_009
  - BENCH_00_00072_010
  - BENCH_00_00072_011
  - BENCH_00_00072_012
  - BENCH_00_00072_013
  - BENCH_00_00072_014
  - BENCH_00_00072_015
  - BENCH_00_00072_016
  - BENCH_00_00072_017
  - BENCH_00_00072_018
  - BENCH_00_00072_019
  - BENCH_00_00072_020
  - BENCH_00_00072_021
  - BENCH_00_00072_022
  - BENCH_00_00072_023
  - BENCH_00_00072_024
  - BENCH_00_00072_025
  - BENCH_00_00072_026
  - BENCH_00_00072_027
  - BENCH_00_00072_028
  - BENCH_00_00072_029
  - BENCH_00_00072_030
  - BENCH_00_00072_031
  - BENCH_00_00072_032
  - BENCH_00_00072_033
  - BENCH_00_00072_034
  - BENCH_00_00072_035
  - BENCH_00_00072_036
  - BENCH_00_00072_037
  - BENCH_00_00072_038
  - BENCH_00_00072_039
  - BENCH_00_00072_040
  - bench-domain-00
  - benchmark
related_domains:
  - bench-domain-00
  - global
acl: public
---

# Benchmark Document 00072

This generated document belongs to `bench-domain-00` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_00_00072_001

Benchmark 标识 `BENCH_00_00072_001` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/001`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_001`、`metric_001_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_00_00072_002

Benchmark 标识 `BENCH_00_00072_002` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/002`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_002`、`metric_002_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_00_00072_003

Benchmark 标识 `BENCH_00_00072_003` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/003`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_003`、`metric_003_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_00_00072_004

Benchmark 标识 `BENCH_00_00072_004` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/004`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_004`、`metric_004_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_00_00072_005

Benchmark 标识 `BENCH_00_00072_005` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/005`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_005`、`metric_005_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_00_00072_006

Benchmark 标识 `BENCH_00_00072_006` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/006`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_006`、`metric_006_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_00_00072_007

Benchmark 标识 `BENCH_00_00072_007` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/007`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_007`、`metric_007_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_00_00072_008

Benchmark 标识 `BENCH_00_00072_008` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/008`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_008`、`metric_008_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_00_00072_009

Benchmark 标识 `BENCH_00_00072_009` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/009`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_009`、`metric_009_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_00_00072_010

Benchmark 标识 `BENCH_00_00072_010` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/010`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_010`、`metric_010_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_00_00072_011

Benchmark 标识 `BENCH_00_00072_011` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/011`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_011`、`metric_011_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_00_00072_012

Benchmark 标识 `BENCH_00_00072_012` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/012`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_012`、`metric_012_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_00_00072_013

Benchmark 标识 `BENCH_00_00072_013` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/013`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_013`、`metric_013_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_00_00072_014

Benchmark 标识 `BENCH_00_00072_014` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/014`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_014`、`metric_014_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_00_00072_015

Benchmark 标识 `BENCH_00_00072_015` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/015`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_015`、`metric_015_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_00_00072_016

Benchmark 标识 `BENCH_00_00072_016` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/016`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_016`、`metric_016_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_00_00072_017

Benchmark 标识 `BENCH_00_00072_017` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/017`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_017`、`metric_017_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_00_00072_018

Benchmark 标识 `BENCH_00_00072_018` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/018`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_018`、`metric_018_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_00_00072_019

Benchmark 标识 `BENCH_00_00072_019` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/019`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_019`、`metric_019_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_00_00072_020

Benchmark 标识 `BENCH_00_00072_020` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/020`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_020`、`metric_020_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_00_00072_021

Benchmark 标识 `BENCH_00_00072_021` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/021`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_021`、`metric_021_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_00_00072_022

Benchmark 标识 `BENCH_00_00072_022` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/022`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_022`、`metric_022_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_00_00072_023

Benchmark 标识 `BENCH_00_00072_023` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/023`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_023`、`metric_023_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_00_00072_024

Benchmark 标识 `BENCH_00_00072_024` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/024`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_024`、`metric_024_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_00_00072_025

Benchmark 标识 `BENCH_00_00072_025` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/025`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_025`、`metric_025_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_00_00072_026

Benchmark 标识 `BENCH_00_00072_026` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/026`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_026`、`metric_026_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_00_00072_027

Benchmark 标识 `BENCH_00_00072_027` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/027`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_027`、`metric_027_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_00_00072_028

Benchmark 标识 `BENCH_00_00072_028` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/028`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_028`、`metric_028_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_00_00072_029

Benchmark 标识 `BENCH_00_00072_029` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/029`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_029`、`metric_029_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_00_00072_030

Benchmark 标识 `BENCH_00_00072_030` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/030`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_030`、`metric_030_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_00_00072_031

Benchmark 标识 `BENCH_00_00072_031` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/031`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_031`、`metric_031_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_00_00072_032

Benchmark 标识 `BENCH_00_00072_032` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/032`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_032`、`metric_032_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_00_00072_033

Benchmark 标识 `BENCH_00_00072_033` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/033`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_033`、`metric_033_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_00_00072_034

Benchmark 标识 `BENCH_00_00072_034` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/034`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_034`、`metric_034_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_00_00072_035

Benchmark 标识 `BENCH_00_00072_035` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/035`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_035`、`metric_035_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_00_00072_036

Benchmark 标识 `BENCH_00_00072_036` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/036`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_036`、`metric_036_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_00_00072_037

Benchmark 标识 `BENCH_00_00072_037` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/037`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_037`、`metric_037_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_00_00072_038

Benchmark 标识 `BENCH_00_00072_038` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/038`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_038`、`metric_038_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_00_00072_039

Benchmark 标识 `BENCH_00_00072_039` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/039`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_039`、`metric_039_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_00_00072_040

Benchmark 标识 `BENCH_00_00072_040` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00072/040`。
核心表 `t_bench_bench_domain_00_72` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B072040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00072_040`、`metric_040_value`、`t_bench_bench_domain_00_72` 三个关键词是否同时出现在上下文。
