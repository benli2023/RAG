---
title: Benchmark Document 00024
domain: bench-domain-00
type: reference
summary: Synthetic benchmark document 00024 for bench-domain-00. Tokens include BENCH_00_00024_001 BENCH_00_00024_002 BENCH_00_00024_003 BENCH_00_00024_004 BENCH_00_00024_005.
keywords:
  - BENCH_00_00024_001
  - BENCH_00_00024_002
  - BENCH_00_00024_003
  - BENCH_00_00024_004
  - BENCH_00_00024_005
  - BENCH_00_00024_006
  - BENCH_00_00024_007
  - BENCH_00_00024_008
  - BENCH_00_00024_009
  - BENCH_00_00024_010
  - BENCH_00_00024_011
  - BENCH_00_00024_012
  - BENCH_00_00024_013
  - BENCH_00_00024_014
  - BENCH_00_00024_015
  - BENCH_00_00024_016
  - BENCH_00_00024_017
  - BENCH_00_00024_018
  - BENCH_00_00024_019
  - BENCH_00_00024_020
  - BENCH_00_00024_021
  - BENCH_00_00024_022
  - BENCH_00_00024_023
  - BENCH_00_00024_024
  - BENCH_00_00024_025
  - BENCH_00_00024_026
  - BENCH_00_00024_027
  - BENCH_00_00024_028
  - BENCH_00_00024_029
  - BENCH_00_00024_030
  - BENCH_00_00024_031
  - BENCH_00_00024_032
  - BENCH_00_00024_033
  - BENCH_00_00024_034
  - BENCH_00_00024_035
  - BENCH_00_00024_036
  - BENCH_00_00024_037
  - BENCH_00_00024_038
  - BENCH_00_00024_039
  - BENCH_00_00024_040
  - bench-domain-00
  - benchmark
related_domains:
  - bench-domain-00
  - global
acl: public
---

# Benchmark Document 00024

This generated document belongs to `bench-domain-00` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_00_00024_001

Benchmark 标识 `BENCH_00_00024_001` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/001`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_001`、`metric_001_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_00_00024_002

Benchmark 标识 `BENCH_00_00024_002` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/002`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_002`、`metric_002_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_00_00024_003

Benchmark 标识 `BENCH_00_00024_003` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/003`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_003`、`metric_003_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_00_00024_004

Benchmark 标识 `BENCH_00_00024_004` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/004`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_004`、`metric_004_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_00_00024_005

Benchmark 标识 `BENCH_00_00024_005` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/005`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_005`、`metric_005_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_00_00024_006

Benchmark 标识 `BENCH_00_00024_006` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/006`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_006`、`metric_006_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_00_00024_007

Benchmark 标识 `BENCH_00_00024_007` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/007`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_007`、`metric_007_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_00_00024_008

Benchmark 标识 `BENCH_00_00024_008` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/008`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_008`、`metric_008_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_00_00024_009

Benchmark 标识 `BENCH_00_00024_009` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/009`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_009`、`metric_009_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_00_00024_010

Benchmark 标识 `BENCH_00_00024_010` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/010`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_010`、`metric_010_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_00_00024_011

Benchmark 标识 `BENCH_00_00024_011` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/011`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_011`、`metric_011_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_00_00024_012

Benchmark 标识 `BENCH_00_00024_012` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/012`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_012`、`metric_012_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_00_00024_013

Benchmark 标识 `BENCH_00_00024_013` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/013`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_013`、`metric_013_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_00_00024_014

Benchmark 标识 `BENCH_00_00024_014` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/014`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_014`、`metric_014_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_00_00024_015

Benchmark 标识 `BENCH_00_00024_015` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/015`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_015`、`metric_015_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_00_00024_016

Benchmark 标识 `BENCH_00_00024_016` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/016`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_016`、`metric_016_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_00_00024_017

Benchmark 标识 `BENCH_00_00024_017` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/017`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_017`、`metric_017_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_00_00024_018

Benchmark 标识 `BENCH_00_00024_018` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/018`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_018`、`metric_018_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_00_00024_019

Benchmark 标识 `BENCH_00_00024_019` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/019`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_019`、`metric_019_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_00_00024_020

Benchmark 标识 `BENCH_00_00024_020` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/020`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_020`、`metric_020_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_00_00024_021

Benchmark 标识 `BENCH_00_00024_021` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/021`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_021`、`metric_021_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_00_00024_022

Benchmark 标识 `BENCH_00_00024_022` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/022`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_022`、`metric_022_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_00_00024_023

Benchmark 标识 `BENCH_00_00024_023` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/023`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_023`、`metric_023_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_00_00024_024

Benchmark 标识 `BENCH_00_00024_024` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/024`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_024`、`metric_024_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_00_00024_025

Benchmark 标识 `BENCH_00_00024_025` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/025`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_025`、`metric_025_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_00_00024_026

Benchmark 标识 `BENCH_00_00024_026` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/026`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_026`、`metric_026_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_00_00024_027

Benchmark 标识 `BENCH_00_00024_027` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/027`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_027`、`metric_027_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_00_00024_028

Benchmark 标识 `BENCH_00_00024_028` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/028`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_028`、`metric_028_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_00_00024_029

Benchmark 标识 `BENCH_00_00024_029` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/029`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_029`、`metric_029_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_00_00024_030

Benchmark 标识 `BENCH_00_00024_030` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/030`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_030`、`metric_030_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_00_00024_031

Benchmark 标识 `BENCH_00_00024_031` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/031`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_031`、`metric_031_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_00_00024_032

Benchmark 标识 `BENCH_00_00024_032` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/032`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_032`、`metric_032_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_00_00024_033

Benchmark 标识 `BENCH_00_00024_033` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/033`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_033`、`metric_033_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_00_00024_034

Benchmark 标识 `BENCH_00_00024_034` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/034`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_034`、`metric_034_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_00_00024_035

Benchmark 标识 `BENCH_00_00024_035` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/035`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_035`、`metric_035_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_00_00024_036

Benchmark 标识 `BENCH_00_00024_036` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/036`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_036`、`metric_036_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_00_00024_037

Benchmark 标识 `BENCH_00_00024_037` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/037`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_037`、`metric_037_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_00_00024_038

Benchmark 标识 `BENCH_00_00024_038` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/038`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_038`、`metric_038_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_00_00024_039

Benchmark 标识 `BENCH_00_00024_039` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/039`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_039`、`metric_039_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_00_00024_040

Benchmark 标识 `BENCH_00_00024_040` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00024/040`。
核心表 `t_bench_bench_domain_00_24` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B024040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00024_040`、`metric_040_value`、`t_bench_bench_domain_00_24` 三个关键词是否同时出现在上下文。
