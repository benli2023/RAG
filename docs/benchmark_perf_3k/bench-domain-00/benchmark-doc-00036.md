---
title: Benchmark Document 00036
domain: bench-domain-00
type: reference
summary: Synthetic benchmark document 00036 for bench-domain-00. Tokens include BENCH_00_00036_001 BENCH_00_00036_002 BENCH_00_00036_003 BENCH_00_00036_004 BENCH_00_00036_005.
keywords:
  - BENCH_00_00036_001
  - BENCH_00_00036_002
  - BENCH_00_00036_003
  - BENCH_00_00036_004
  - BENCH_00_00036_005
  - BENCH_00_00036_006
  - BENCH_00_00036_007
  - BENCH_00_00036_008
  - BENCH_00_00036_009
  - BENCH_00_00036_010
  - BENCH_00_00036_011
  - BENCH_00_00036_012
  - BENCH_00_00036_013
  - BENCH_00_00036_014
  - BENCH_00_00036_015
  - BENCH_00_00036_016
  - BENCH_00_00036_017
  - BENCH_00_00036_018
  - BENCH_00_00036_019
  - BENCH_00_00036_020
  - BENCH_00_00036_021
  - BENCH_00_00036_022
  - BENCH_00_00036_023
  - BENCH_00_00036_024
  - BENCH_00_00036_025
  - BENCH_00_00036_026
  - BENCH_00_00036_027
  - BENCH_00_00036_028
  - BENCH_00_00036_029
  - BENCH_00_00036_030
  - BENCH_00_00036_031
  - BENCH_00_00036_032
  - BENCH_00_00036_033
  - BENCH_00_00036_034
  - BENCH_00_00036_035
  - BENCH_00_00036_036
  - BENCH_00_00036_037
  - BENCH_00_00036_038
  - BENCH_00_00036_039
  - BENCH_00_00036_040
  - bench-domain-00
  - benchmark
related_domains:
  - bench-domain-00
  - global
acl: public
---

# Benchmark Document 00036

This generated document belongs to `bench-domain-00` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_00_00036_001

Benchmark 标识 `BENCH_00_00036_001` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/001`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_001`、`metric_001_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_00_00036_002

Benchmark 标识 `BENCH_00_00036_002` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/002`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_002`、`metric_002_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_00_00036_003

Benchmark 标识 `BENCH_00_00036_003` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/003`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_003`、`metric_003_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_00_00036_004

Benchmark 标识 `BENCH_00_00036_004` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/004`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_004`、`metric_004_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_00_00036_005

Benchmark 标识 `BENCH_00_00036_005` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/005`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_005`、`metric_005_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_00_00036_006

Benchmark 标识 `BENCH_00_00036_006` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/006`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_006`、`metric_006_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_00_00036_007

Benchmark 标识 `BENCH_00_00036_007` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/007`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_007`、`metric_007_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_00_00036_008

Benchmark 标识 `BENCH_00_00036_008` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/008`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_008`、`metric_008_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_00_00036_009

Benchmark 标识 `BENCH_00_00036_009` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/009`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_009`、`metric_009_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_00_00036_010

Benchmark 标识 `BENCH_00_00036_010` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/010`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_010`、`metric_010_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_00_00036_011

Benchmark 标识 `BENCH_00_00036_011` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/011`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_011`、`metric_011_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_00_00036_012

Benchmark 标识 `BENCH_00_00036_012` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/012`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_012`、`metric_012_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_00_00036_013

Benchmark 标识 `BENCH_00_00036_013` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/013`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_013`、`metric_013_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_00_00036_014

Benchmark 标识 `BENCH_00_00036_014` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/014`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_014`、`metric_014_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_00_00036_015

Benchmark 标识 `BENCH_00_00036_015` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/015`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_015`、`metric_015_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_00_00036_016

Benchmark 标识 `BENCH_00_00036_016` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/016`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_016`、`metric_016_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_00_00036_017

Benchmark 标识 `BENCH_00_00036_017` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/017`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_017`、`metric_017_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_00_00036_018

Benchmark 标识 `BENCH_00_00036_018` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/018`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_018`、`metric_018_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_00_00036_019

Benchmark 标识 `BENCH_00_00036_019` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/019`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_019`、`metric_019_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_00_00036_020

Benchmark 标识 `BENCH_00_00036_020` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/020`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_020`、`metric_020_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_00_00036_021

Benchmark 标识 `BENCH_00_00036_021` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/021`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_021`、`metric_021_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_00_00036_022

Benchmark 标识 `BENCH_00_00036_022` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/022`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_022`、`metric_022_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_00_00036_023

Benchmark 标识 `BENCH_00_00036_023` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/023`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_023`、`metric_023_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_00_00036_024

Benchmark 标识 `BENCH_00_00036_024` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/024`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_024`、`metric_024_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_00_00036_025

Benchmark 标识 `BENCH_00_00036_025` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/025`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_025`、`metric_025_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_00_00036_026

Benchmark 标识 `BENCH_00_00036_026` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/026`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_026`、`metric_026_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_00_00036_027

Benchmark 标识 `BENCH_00_00036_027` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/027`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_027`、`metric_027_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_00_00036_028

Benchmark 标识 `BENCH_00_00036_028` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/028`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_028`、`metric_028_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_00_00036_029

Benchmark 标识 `BENCH_00_00036_029` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/029`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_029`、`metric_029_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_00_00036_030

Benchmark 标识 `BENCH_00_00036_030` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/030`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_030`、`metric_030_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_00_00036_031

Benchmark 标识 `BENCH_00_00036_031` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/031`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_031`、`metric_031_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_00_00036_032

Benchmark 标识 `BENCH_00_00036_032` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/032`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_032`、`metric_032_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_00_00036_033

Benchmark 标识 `BENCH_00_00036_033` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/033`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_033`、`metric_033_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_00_00036_034

Benchmark 标识 `BENCH_00_00036_034` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/034`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_034`、`metric_034_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_00_00036_035

Benchmark 标识 `BENCH_00_00036_035` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/035`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_035`、`metric_035_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_00_00036_036

Benchmark 标识 `BENCH_00_00036_036` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/036`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_036`、`metric_036_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_00_00036_037

Benchmark 标识 `BENCH_00_00036_037` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/037`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_037`、`metric_037_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_00_00036_038

Benchmark 标识 `BENCH_00_00036_038` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/038`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_038`、`metric_038_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_00_00036_039

Benchmark 标识 `BENCH_00_00036_039` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/039`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_039`、`metric_039_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_00_00036_040

Benchmark 标识 `BENCH_00_00036_040` 属于 `bench-domain-00` 模块，接口路径是 `/api/benchmark/bench-domain-00/case/00036/040`。
核心表 `t_bench_bench_domain_00_36` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B036040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_00_00036_040`、`metric_040_value`、`t_bench_bench_domain_00_36` 三个关键词是否同时出现在上下文。
