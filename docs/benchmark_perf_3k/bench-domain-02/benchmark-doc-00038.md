---
title: Benchmark Document 00038
domain: bench-domain-02
type: reference
summary: Synthetic benchmark document 00038 for bench-domain-02. Tokens include BENCH_02_00038_001 BENCH_02_00038_002 BENCH_02_00038_003 BENCH_02_00038_004 BENCH_02_00038_005.
keywords:
  - BENCH_02_00038_001
  - BENCH_02_00038_002
  - BENCH_02_00038_003
  - BENCH_02_00038_004
  - BENCH_02_00038_005
  - BENCH_02_00038_006
  - BENCH_02_00038_007
  - BENCH_02_00038_008
  - BENCH_02_00038_009
  - BENCH_02_00038_010
  - BENCH_02_00038_011
  - BENCH_02_00038_012
  - BENCH_02_00038_013
  - BENCH_02_00038_014
  - BENCH_02_00038_015
  - BENCH_02_00038_016
  - BENCH_02_00038_017
  - BENCH_02_00038_018
  - BENCH_02_00038_019
  - BENCH_02_00038_020
  - BENCH_02_00038_021
  - BENCH_02_00038_022
  - BENCH_02_00038_023
  - BENCH_02_00038_024
  - BENCH_02_00038_025
  - BENCH_02_00038_026
  - BENCH_02_00038_027
  - BENCH_02_00038_028
  - BENCH_02_00038_029
  - BENCH_02_00038_030
  - BENCH_02_00038_031
  - BENCH_02_00038_032
  - BENCH_02_00038_033
  - BENCH_02_00038_034
  - BENCH_02_00038_035
  - BENCH_02_00038_036
  - BENCH_02_00038_037
  - BENCH_02_00038_038
  - BENCH_02_00038_039
  - BENCH_02_00038_040
  - bench-domain-02
  - benchmark
related_domains:
  - bench-domain-02
  - global
acl: public
---

# Benchmark Document 00038

This generated document belongs to `bench-domain-02` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_02_00038_001

Benchmark 标识 `BENCH_02_00038_001` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/001`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_001`、`metric_001_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_02_00038_002

Benchmark 标识 `BENCH_02_00038_002` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/002`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_002`、`metric_002_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_02_00038_003

Benchmark 标识 `BENCH_02_00038_003` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/003`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_003`、`metric_003_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_02_00038_004

Benchmark 标识 `BENCH_02_00038_004` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/004`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_004`、`metric_004_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_02_00038_005

Benchmark 标识 `BENCH_02_00038_005` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/005`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_005`、`metric_005_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_02_00038_006

Benchmark 标识 `BENCH_02_00038_006` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/006`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_006`、`metric_006_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_02_00038_007

Benchmark 标识 `BENCH_02_00038_007` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/007`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_007`、`metric_007_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_02_00038_008

Benchmark 标识 `BENCH_02_00038_008` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/008`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_008`、`metric_008_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_02_00038_009

Benchmark 标识 `BENCH_02_00038_009` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/009`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_009`、`metric_009_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_02_00038_010

Benchmark 标识 `BENCH_02_00038_010` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/010`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_010`、`metric_010_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_02_00038_011

Benchmark 标识 `BENCH_02_00038_011` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/011`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_011`、`metric_011_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_02_00038_012

Benchmark 标识 `BENCH_02_00038_012` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/012`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_012`、`metric_012_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_02_00038_013

Benchmark 标识 `BENCH_02_00038_013` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/013`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_013`、`metric_013_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_02_00038_014

Benchmark 标识 `BENCH_02_00038_014` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/014`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_014`、`metric_014_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_02_00038_015

Benchmark 标识 `BENCH_02_00038_015` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/015`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_015`、`metric_015_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_02_00038_016

Benchmark 标识 `BENCH_02_00038_016` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/016`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_016`、`metric_016_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_02_00038_017

Benchmark 标识 `BENCH_02_00038_017` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/017`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_017`、`metric_017_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_02_00038_018

Benchmark 标识 `BENCH_02_00038_018` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/018`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_018`、`metric_018_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_02_00038_019

Benchmark 标识 `BENCH_02_00038_019` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/019`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_019`、`metric_019_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_02_00038_020

Benchmark 标识 `BENCH_02_00038_020` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/020`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_020`、`metric_020_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_02_00038_021

Benchmark 标识 `BENCH_02_00038_021` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/021`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_021`、`metric_021_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_02_00038_022

Benchmark 标识 `BENCH_02_00038_022` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/022`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_022`、`metric_022_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_02_00038_023

Benchmark 标识 `BENCH_02_00038_023` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/023`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_023`、`metric_023_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_02_00038_024

Benchmark 标识 `BENCH_02_00038_024` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/024`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_024`、`metric_024_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_02_00038_025

Benchmark 标识 `BENCH_02_00038_025` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/025`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_025`、`metric_025_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_02_00038_026

Benchmark 标识 `BENCH_02_00038_026` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/026`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_026`、`metric_026_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_02_00038_027

Benchmark 标识 `BENCH_02_00038_027` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/027`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_027`、`metric_027_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_02_00038_028

Benchmark 标识 `BENCH_02_00038_028` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/028`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_028`、`metric_028_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_02_00038_029

Benchmark 标识 `BENCH_02_00038_029` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/029`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_029`、`metric_029_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_02_00038_030

Benchmark 标识 `BENCH_02_00038_030` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/030`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_030`、`metric_030_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_02_00038_031

Benchmark 标识 `BENCH_02_00038_031` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/031`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_031`、`metric_031_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_02_00038_032

Benchmark 标识 `BENCH_02_00038_032` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/032`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_032`、`metric_032_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_02_00038_033

Benchmark 标识 `BENCH_02_00038_033` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/033`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_033`、`metric_033_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_02_00038_034

Benchmark 标识 `BENCH_02_00038_034` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/034`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_034`、`metric_034_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_02_00038_035

Benchmark 标识 `BENCH_02_00038_035` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/035`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_035`、`metric_035_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_02_00038_036

Benchmark 标识 `BENCH_02_00038_036` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/036`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_036`、`metric_036_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_02_00038_037

Benchmark 标识 `BENCH_02_00038_037` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/037`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_037`、`metric_037_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_02_00038_038

Benchmark 标识 `BENCH_02_00038_038` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/038`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_038`、`metric_038_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_02_00038_039

Benchmark 标识 `BENCH_02_00038_039` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/039`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_039`、`metric_039_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_02_00038_040

Benchmark 标识 `BENCH_02_00038_040` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00038/040`。
核心表 `t_bench_bench_domain_02_38` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B038040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00038_040`、`metric_040_value`、`t_bench_bench_domain_02_38` 三个关键词是否同时出现在上下文。
