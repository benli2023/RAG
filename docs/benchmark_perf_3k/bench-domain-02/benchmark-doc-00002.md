---
title: Benchmark Document 00002
domain: bench-domain-02
type: reference
summary: Synthetic benchmark document 00002 for bench-domain-02. Tokens include BENCH_02_00002_001 BENCH_02_00002_002 BENCH_02_00002_003 BENCH_02_00002_004 BENCH_02_00002_005.
keywords:
  - BENCH_02_00002_001
  - BENCH_02_00002_002
  - BENCH_02_00002_003
  - BENCH_02_00002_004
  - BENCH_02_00002_005
  - BENCH_02_00002_006
  - BENCH_02_00002_007
  - BENCH_02_00002_008
  - BENCH_02_00002_009
  - BENCH_02_00002_010
  - BENCH_02_00002_011
  - BENCH_02_00002_012
  - BENCH_02_00002_013
  - BENCH_02_00002_014
  - BENCH_02_00002_015
  - BENCH_02_00002_016
  - BENCH_02_00002_017
  - BENCH_02_00002_018
  - BENCH_02_00002_019
  - BENCH_02_00002_020
  - BENCH_02_00002_021
  - BENCH_02_00002_022
  - BENCH_02_00002_023
  - BENCH_02_00002_024
  - BENCH_02_00002_025
  - BENCH_02_00002_026
  - BENCH_02_00002_027
  - BENCH_02_00002_028
  - BENCH_02_00002_029
  - BENCH_02_00002_030
  - BENCH_02_00002_031
  - BENCH_02_00002_032
  - BENCH_02_00002_033
  - BENCH_02_00002_034
  - BENCH_02_00002_035
  - BENCH_02_00002_036
  - BENCH_02_00002_037
  - BENCH_02_00002_038
  - BENCH_02_00002_039
  - BENCH_02_00002_040
  - bench-domain-02
  - benchmark
related_domains:
  - bench-domain-02
  - global
acl: public
---

# Benchmark Document 00002

This generated document belongs to `bench-domain-02` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_02_00002_001

Benchmark 标识 `BENCH_02_00002_001` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/001`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_001`、`metric_001_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_02_00002_002

Benchmark 标识 `BENCH_02_00002_002` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/002`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_002`、`metric_002_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_02_00002_003

Benchmark 标识 `BENCH_02_00002_003` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/003`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_003`、`metric_003_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_02_00002_004

Benchmark 标识 `BENCH_02_00002_004` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/004`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_004`、`metric_004_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_02_00002_005

Benchmark 标识 `BENCH_02_00002_005` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/005`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_005`、`metric_005_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_02_00002_006

Benchmark 标识 `BENCH_02_00002_006` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/006`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_006`、`metric_006_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_02_00002_007

Benchmark 标识 `BENCH_02_00002_007` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/007`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_007`、`metric_007_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_02_00002_008

Benchmark 标识 `BENCH_02_00002_008` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/008`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_008`、`metric_008_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_02_00002_009

Benchmark 标识 `BENCH_02_00002_009` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/009`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_009`、`metric_009_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_02_00002_010

Benchmark 标识 `BENCH_02_00002_010` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/010`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_010`、`metric_010_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_02_00002_011

Benchmark 标识 `BENCH_02_00002_011` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/011`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_011`、`metric_011_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_02_00002_012

Benchmark 标识 `BENCH_02_00002_012` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/012`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_012`、`metric_012_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_02_00002_013

Benchmark 标识 `BENCH_02_00002_013` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/013`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_013`、`metric_013_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_02_00002_014

Benchmark 标识 `BENCH_02_00002_014` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/014`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_014`、`metric_014_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_02_00002_015

Benchmark 标识 `BENCH_02_00002_015` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/015`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_015`、`metric_015_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_02_00002_016

Benchmark 标识 `BENCH_02_00002_016` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/016`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_016`、`metric_016_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_02_00002_017

Benchmark 标识 `BENCH_02_00002_017` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/017`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_017`、`metric_017_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_02_00002_018

Benchmark 标识 `BENCH_02_00002_018` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/018`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_018`、`metric_018_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_02_00002_019

Benchmark 标识 `BENCH_02_00002_019` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/019`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_019`、`metric_019_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_02_00002_020

Benchmark 标识 `BENCH_02_00002_020` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/020`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_020`、`metric_020_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_02_00002_021

Benchmark 标识 `BENCH_02_00002_021` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/021`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_021`、`metric_021_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_02_00002_022

Benchmark 标识 `BENCH_02_00002_022` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/022`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_022`、`metric_022_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_02_00002_023

Benchmark 标识 `BENCH_02_00002_023` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/023`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_023`、`metric_023_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_02_00002_024

Benchmark 标识 `BENCH_02_00002_024` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/024`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_024`、`metric_024_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_02_00002_025

Benchmark 标识 `BENCH_02_00002_025` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/025`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_025`、`metric_025_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_02_00002_026

Benchmark 标识 `BENCH_02_00002_026` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/026`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_026`、`metric_026_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_02_00002_027

Benchmark 标识 `BENCH_02_00002_027` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/027`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_027`、`metric_027_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_02_00002_028

Benchmark 标识 `BENCH_02_00002_028` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/028`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_028`、`metric_028_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_02_00002_029

Benchmark 标识 `BENCH_02_00002_029` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/029`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_029`、`metric_029_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_02_00002_030

Benchmark 标识 `BENCH_02_00002_030` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/030`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_030`、`metric_030_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_02_00002_031

Benchmark 标识 `BENCH_02_00002_031` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/031`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_031`、`metric_031_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_02_00002_032

Benchmark 标识 `BENCH_02_00002_032` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/032`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_032`、`metric_032_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_02_00002_033

Benchmark 标识 `BENCH_02_00002_033` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/033`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_033`、`metric_033_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_02_00002_034

Benchmark 标识 `BENCH_02_00002_034` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/034`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_034`、`metric_034_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_02_00002_035

Benchmark 标识 `BENCH_02_00002_035` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/035`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_035`、`metric_035_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_02_00002_036

Benchmark 标识 `BENCH_02_00002_036` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/036`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_036`、`metric_036_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_02_00002_037

Benchmark 标识 `BENCH_02_00002_037` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/037`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_037`、`metric_037_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_02_00002_038

Benchmark 标识 `BENCH_02_00002_038` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/038`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_038`、`metric_038_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_02_00002_039

Benchmark 标识 `BENCH_02_00002_039` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/039`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_039`、`metric_039_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_02_00002_040

Benchmark 标识 `BENCH_02_00002_040` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00002/040`。
核心表 `t_bench_bench_domain_02_02` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B002040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00002_040`、`metric_040_value`、`t_bench_bench_domain_02_02` 三个关键词是否同时出现在上下文。
