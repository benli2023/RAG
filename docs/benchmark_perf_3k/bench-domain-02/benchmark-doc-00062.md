---
title: Benchmark Document 00062
domain: bench-domain-02
type: reference
summary: Synthetic benchmark document 00062 for bench-domain-02. Tokens include BENCH_02_00062_001 BENCH_02_00062_002 BENCH_02_00062_003 BENCH_02_00062_004 BENCH_02_00062_005.
keywords:
  - BENCH_02_00062_001
  - BENCH_02_00062_002
  - BENCH_02_00062_003
  - BENCH_02_00062_004
  - BENCH_02_00062_005
  - BENCH_02_00062_006
  - BENCH_02_00062_007
  - BENCH_02_00062_008
  - BENCH_02_00062_009
  - BENCH_02_00062_010
  - BENCH_02_00062_011
  - BENCH_02_00062_012
  - BENCH_02_00062_013
  - BENCH_02_00062_014
  - BENCH_02_00062_015
  - BENCH_02_00062_016
  - BENCH_02_00062_017
  - BENCH_02_00062_018
  - BENCH_02_00062_019
  - BENCH_02_00062_020
  - BENCH_02_00062_021
  - BENCH_02_00062_022
  - BENCH_02_00062_023
  - BENCH_02_00062_024
  - BENCH_02_00062_025
  - BENCH_02_00062_026
  - BENCH_02_00062_027
  - BENCH_02_00062_028
  - BENCH_02_00062_029
  - BENCH_02_00062_030
  - BENCH_02_00062_031
  - BENCH_02_00062_032
  - BENCH_02_00062_033
  - BENCH_02_00062_034
  - BENCH_02_00062_035
  - BENCH_02_00062_036
  - BENCH_02_00062_037
  - BENCH_02_00062_038
  - BENCH_02_00062_039
  - BENCH_02_00062_040
  - bench-domain-02
  - benchmark
related_domains:
  - bench-domain-02
  - global
acl: public
---

# Benchmark Document 00062

This generated document belongs to `bench-domain-02` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_02_00062_001

Benchmark 标识 `BENCH_02_00062_001` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/001`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_001`、`metric_001_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_02_00062_002

Benchmark 标识 `BENCH_02_00062_002` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/002`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_002`、`metric_002_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_02_00062_003

Benchmark 标识 `BENCH_02_00062_003` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/003`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_003`、`metric_003_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_02_00062_004

Benchmark 标识 `BENCH_02_00062_004` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/004`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_004`、`metric_004_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_02_00062_005

Benchmark 标识 `BENCH_02_00062_005` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/005`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_005`、`metric_005_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_02_00062_006

Benchmark 标识 `BENCH_02_00062_006` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/006`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_006`、`metric_006_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_02_00062_007

Benchmark 标识 `BENCH_02_00062_007` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/007`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_007`、`metric_007_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_02_00062_008

Benchmark 标识 `BENCH_02_00062_008` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/008`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_008`、`metric_008_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_02_00062_009

Benchmark 标识 `BENCH_02_00062_009` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/009`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_009`、`metric_009_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_02_00062_010

Benchmark 标识 `BENCH_02_00062_010` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/010`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_010`、`metric_010_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_02_00062_011

Benchmark 标识 `BENCH_02_00062_011` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/011`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_011`、`metric_011_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_02_00062_012

Benchmark 标识 `BENCH_02_00062_012` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/012`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_012`、`metric_012_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_02_00062_013

Benchmark 标识 `BENCH_02_00062_013` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/013`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_013`、`metric_013_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_02_00062_014

Benchmark 标识 `BENCH_02_00062_014` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/014`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_014`、`metric_014_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_02_00062_015

Benchmark 标识 `BENCH_02_00062_015` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/015`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_015`、`metric_015_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_02_00062_016

Benchmark 标识 `BENCH_02_00062_016` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/016`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_016`、`metric_016_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_02_00062_017

Benchmark 标识 `BENCH_02_00062_017` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/017`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_017`、`metric_017_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_02_00062_018

Benchmark 标识 `BENCH_02_00062_018` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/018`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_018`、`metric_018_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_02_00062_019

Benchmark 标识 `BENCH_02_00062_019` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/019`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_019`、`metric_019_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_02_00062_020

Benchmark 标识 `BENCH_02_00062_020` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/020`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_020`、`metric_020_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_02_00062_021

Benchmark 标识 `BENCH_02_00062_021` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/021`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_021`、`metric_021_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_02_00062_022

Benchmark 标识 `BENCH_02_00062_022` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/022`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_022`、`metric_022_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_02_00062_023

Benchmark 标识 `BENCH_02_00062_023` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/023`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_023`、`metric_023_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_02_00062_024

Benchmark 标识 `BENCH_02_00062_024` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/024`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_024`、`metric_024_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_02_00062_025

Benchmark 标识 `BENCH_02_00062_025` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/025`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_025`、`metric_025_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_02_00062_026

Benchmark 标识 `BENCH_02_00062_026` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/026`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_026`、`metric_026_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_02_00062_027

Benchmark 标识 `BENCH_02_00062_027` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/027`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_027`、`metric_027_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_02_00062_028

Benchmark 标识 `BENCH_02_00062_028` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/028`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_028`、`metric_028_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_02_00062_029

Benchmark 标识 `BENCH_02_00062_029` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/029`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_029`、`metric_029_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_02_00062_030

Benchmark 标识 `BENCH_02_00062_030` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/030`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_030`、`metric_030_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_02_00062_031

Benchmark 标识 `BENCH_02_00062_031` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/031`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_031`、`metric_031_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_02_00062_032

Benchmark 标识 `BENCH_02_00062_032` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/032`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_032`、`metric_032_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_02_00062_033

Benchmark 标识 `BENCH_02_00062_033` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/033`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_033`、`metric_033_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_02_00062_034

Benchmark 标识 `BENCH_02_00062_034` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/034`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_034`、`metric_034_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_02_00062_035

Benchmark 标识 `BENCH_02_00062_035` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/035`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_035`、`metric_035_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_02_00062_036

Benchmark 标识 `BENCH_02_00062_036` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/036`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_036`、`metric_036_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_02_00062_037

Benchmark 标识 `BENCH_02_00062_037` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/037`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_037`、`metric_037_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_02_00062_038

Benchmark 标识 `BENCH_02_00062_038` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/038`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_038`、`metric_038_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_02_00062_039

Benchmark 标识 `BENCH_02_00062_039` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/039`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_039`、`metric_039_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_02_00062_040

Benchmark 标识 `BENCH_02_00062_040` 属于 `bench-domain-02` 模块，接口路径是 `/api/benchmark/bench-domain-02/case/00062/040`。
核心表 `t_bench_bench_domain_02_62` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B062040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_02_00062_040`、`metric_040_value`、`t_bench_bench_domain_02_62` 三个关键词是否同时出现在上下文。
