---
title: Benchmark Document 00046
domain: bench-domain-10
type: reference
summary: Synthetic benchmark document 00046 for bench-domain-10. Tokens include BENCH_10_00046_001 BENCH_10_00046_002 BENCH_10_00046_003 BENCH_10_00046_004 BENCH_10_00046_005.
keywords:
  - BENCH_10_00046_001
  - BENCH_10_00046_002
  - BENCH_10_00046_003
  - BENCH_10_00046_004
  - BENCH_10_00046_005
  - BENCH_10_00046_006
  - BENCH_10_00046_007
  - BENCH_10_00046_008
  - BENCH_10_00046_009
  - BENCH_10_00046_010
  - BENCH_10_00046_011
  - BENCH_10_00046_012
  - BENCH_10_00046_013
  - BENCH_10_00046_014
  - BENCH_10_00046_015
  - BENCH_10_00046_016
  - BENCH_10_00046_017
  - BENCH_10_00046_018
  - BENCH_10_00046_019
  - BENCH_10_00046_020
  - BENCH_10_00046_021
  - BENCH_10_00046_022
  - BENCH_10_00046_023
  - BENCH_10_00046_024
  - BENCH_10_00046_025
  - BENCH_10_00046_026
  - BENCH_10_00046_027
  - BENCH_10_00046_028
  - BENCH_10_00046_029
  - BENCH_10_00046_030
  - BENCH_10_00046_031
  - BENCH_10_00046_032
  - BENCH_10_00046_033
  - BENCH_10_00046_034
  - BENCH_10_00046_035
  - BENCH_10_00046_036
  - BENCH_10_00046_037
  - BENCH_10_00046_038
  - BENCH_10_00046_039
  - BENCH_10_00046_040
  - bench-domain-10
  - benchmark
related_domains:
  - bench-domain-10
  - global
acl: public
---

# Benchmark Document 00046

This generated document belongs to `bench-domain-10` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_10_00046_001

Benchmark 标识 `BENCH_10_00046_001` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/001`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_001`、`metric_001_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_10_00046_002

Benchmark 标识 `BENCH_10_00046_002` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/002`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_002`、`metric_002_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_10_00046_003

Benchmark 标识 `BENCH_10_00046_003` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/003`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_003`、`metric_003_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_10_00046_004

Benchmark 标识 `BENCH_10_00046_004` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/004`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_004`、`metric_004_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_10_00046_005

Benchmark 标识 `BENCH_10_00046_005` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/005`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_005`、`metric_005_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_10_00046_006

Benchmark 标识 `BENCH_10_00046_006` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/006`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_006`、`metric_006_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_10_00046_007

Benchmark 标识 `BENCH_10_00046_007` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/007`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_007`、`metric_007_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_10_00046_008

Benchmark 标识 `BENCH_10_00046_008` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/008`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_008`、`metric_008_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_10_00046_009

Benchmark 标识 `BENCH_10_00046_009` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/009`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_009`、`metric_009_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_10_00046_010

Benchmark 标识 `BENCH_10_00046_010` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/010`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_010`、`metric_010_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_10_00046_011

Benchmark 标识 `BENCH_10_00046_011` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/011`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_011`、`metric_011_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_10_00046_012

Benchmark 标识 `BENCH_10_00046_012` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/012`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_012`、`metric_012_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_10_00046_013

Benchmark 标识 `BENCH_10_00046_013` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/013`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_013`、`metric_013_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_10_00046_014

Benchmark 标识 `BENCH_10_00046_014` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/014`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_014`、`metric_014_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_10_00046_015

Benchmark 标识 `BENCH_10_00046_015` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/015`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_015`、`metric_015_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_10_00046_016

Benchmark 标识 `BENCH_10_00046_016` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/016`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_016`、`metric_016_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_10_00046_017

Benchmark 标识 `BENCH_10_00046_017` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/017`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_017`、`metric_017_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_10_00046_018

Benchmark 标识 `BENCH_10_00046_018` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/018`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_018`、`metric_018_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_10_00046_019

Benchmark 标识 `BENCH_10_00046_019` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/019`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_019`、`metric_019_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_10_00046_020

Benchmark 标识 `BENCH_10_00046_020` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/020`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_020`、`metric_020_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_10_00046_021

Benchmark 标识 `BENCH_10_00046_021` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/021`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_021`、`metric_021_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_10_00046_022

Benchmark 标识 `BENCH_10_00046_022` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/022`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_022`、`metric_022_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_10_00046_023

Benchmark 标识 `BENCH_10_00046_023` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/023`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_023`、`metric_023_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_10_00046_024

Benchmark 标识 `BENCH_10_00046_024` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/024`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_024`、`metric_024_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_10_00046_025

Benchmark 标识 `BENCH_10_00046_025` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/025`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_025`、`metric_025_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_10_00046_026

Benchmark 标识 `BENCH_10_00046_026` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/026`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_026`、`metric_026_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_10_00046_027

Benchmark 标识 `BENCH_10_00046_027` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/027`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_027`、`metric_027_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_10_00046_028

Benchmark 标识 `BENCH_10_00046_028` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/028`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_028`、`metric_028_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_10_00046_029

Benchmark 标识 `BENCH_10_00046_029` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/029`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_029`、`metric_029_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_10_00046_030

Benchmark 标识 `BENCH_10_00046_030` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/030`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_030`、`metric_030_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_10_00046_031

Benchmark 标识 `BENCH_10_00046_031` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/031`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_031`、`metric_031_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_10_00046_032

Benchmark 标识 `BENCH_10_00046_032` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/032`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_032`、`metric_032_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_10_00046_033

Benchmark 标识 `BENCH_10_00046_033` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/033`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_033`、`metric_033_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_10_00046_034

Benchmark 标识 `BENCH_10_00046_034` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/034`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_034`、`metric_034_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_10_00046_035

Benchmark 标识 `BENCH_10_00046_035` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/035`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_035`、`metric_035_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_10_00046_036

Benchmark 标识 `BENCH_10_00046_036` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/036`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_036`、`metric_036_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_10_00046_037

Benchmark 标识 `BENCH_10_00046_037` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/037`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_037`、`metric_037_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_10_00046_038

Benchmark 标识 `BENCH_10_00046_038` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/038`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_038`、`metric_038_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_10_00046_039

Benchmark 标识 `BENCH_10_00046_039` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/039`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_039`、`metric_039_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_10_00046_040

Benchmark 标识 `BENCH_10_00046_040` 属于 `bench-domain-10` 模块，接口路径是 `/api/benchmark/bench-domain-10/case/00046/040`。
核心表 `t_bench_bench_domain_10_46` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B046040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_10_00046_040`、`metric_040_value`、`t_bench_bench_domain_10_46` 三个关键词是否同时出现在上下文。
