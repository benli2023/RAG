---
title: Benchmark Document 00066
domain: bench-domain-06
type: reference
summary: Synthetic benchmark document 00066 for bench-domain-06. Tokens include BENCH_06_00066_001 BENCH_06_00066_002 BENCH_06_00066_003 BENCH_06_00066_004 BENCH_06_00066_005.
keywords:
  - BENCH_06_00066_001
  - BENCH_06_00066_002
  - BENCH_06_00066_003
  - BENCH_06_00066_004
  - BENCH_06_00066_005
  - BENCH_06_00066_006
  - BENCH_06_00066_007
  - BENCH_06_00066_008
  - BENCH_06_00066_009
  - BENCH_06_00066_010
  - BENCH_06_00066_011
  - BENCH_06_00066_012
  - BENCH_06_00066_013
  - BENCH_06_00066_014
  - BENCH_06_00066_015
  - BENCH_06_00066_016
  - BENCH_06_00066_017
  - BENCH_06_00066_018
  - BENCH_06_00066_019
  - BENCH_06_00066_020
  - BENCH_06_00066_021
  - BENCH_06_00066_022
  - BENCH_06_00066_023
  - BENCH_06_00066_024
  - BENCH_06_00066_025
  - BENCH_06_00066_026
  - BENCH_06_00066_027
  - BENCH_06_00066_028
  - BENCH_06_00066_029
  - BENCH_06_00066_030
  - BENCH_06_00066_031
  - BENCH_06_00066_032
  - BENCH_06_00066_033
  - BENCH_06_00066_034
  - BENCH_06_00066_035
  - BENCH_06_00066_036
  - BENCH_06_00066_037
  - BENCH_06_00066_038
  - BENCH_06_00066_039
  - BENCH_06_00066_040
  - bench-domain-06
  - benchmark
related_domains:
  - bench-domain-06
  - global
acl: public
---

# Benchmark Document 00066

This generated document belongs to `bench-domain-06` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_06_00066_001

Benchmark 标识 `BENCH_06_00066_001` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/001`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_001`、`metric_001_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_06_00066_002

Benchmark 标识 `BENCH_06_00066_002` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/002`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_002`、`metric_002_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_06_00066_003

Benchmark 标识 `BENCH_06_00066_003` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/003`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_003`、`metric_003_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_06_00066_004

Benchmark 标识 `BENCH_06_00066_004` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/004`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_004`、`metric_004_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_06_00066_005

Benchmark 标识 `BENCH_06_00066_005` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/005`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_005`、`metric_005_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_06_00066_006

Benchmark 标识 `BENCH_06_00066_006` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/006`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_006`、`metric_006_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_06_00066_007

Benchmark 标识 `BENCH_06_00066_007` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/007`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_007`、`metric_007_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_06_00066_008

Benchmark 标识 `BENCH_06_00066_008` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/008`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_008`、`metric_008_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_06_00066_009

Benchmark 标识 `BENCH_06_00066_009` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/009`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_009`、`metric_009_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_06_00066_010

Benchmark 标识 `BENCH_06_00066_010` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/010`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_010`、`metric_010_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_06_00066_011

Benchmark 标识 `BENCH_06_00066_011` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/011`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_011`、`metric_011_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_06_00066_012

Benchmark 标识 `BENCH_06_00066_012` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/012`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_012`、`metric_012_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_06_00066_013

Benchmark 标识 `BENCH_06_00066_013` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/013`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_013`、`metric_013_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_06_00066_014

Benchmark 标识 `BENCH_06_00066_014` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/014`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_014`、`metric_014_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_06_00066_015

Benchmark 标识 `BENCH_06_00066_015` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/015`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_015`、`metric_015_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_06_00066_016

Benchmark 标识 `BENCH_06_00066_016` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/016`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_016`、`metric_016_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_06_00066_017

Benchmark 标识 `BENCH_06_00066_017` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/017`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_017`、`metric_017_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_06_00066_018

Benchmark 标识 `BENCH_06_00066_018` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/018`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_018`、`metric_018_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_06_00066_019

Benchmark 标识 `BENCH_06_00066_019` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/019`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_019`、`metric_019_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_06_00066_020

Benchmark 标识 `BENCH_06_00066_020` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/020`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_020`、`metric_020_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_06_00066_021

Benchmark 标识 `BENCH_06_00066_021` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/021`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_021`、`metric_021_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_06_00066_022

Benchmark 标识 `BENCH_06_00066_022` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/022`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_022`、`metric_022_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_06_00066_023

Benchmark 标识 `BENCH_06_00066_023` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/023`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_023`、`metric_023_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_06_00066_024

Benchmark 标识 `BENCH_06_00066_024` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/024`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_024`、`metric_024_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_06_00066_025

Benchmark 标识 `BENCH_06_00066_025` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/025`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_025`、`metric_025_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_06_00066_026

Benchmark 标识 `BENCH_06_00066_026` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/026`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_026`、`metric_026_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_06_00066_027

Benchmark 标识 `BENCH_06_00066_027` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/027`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_027`、`metric_027_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_06_00066_028

Benchmark 标识 `BENCH_06_00066_028` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/028`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_028`、`metric_028_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_06_00066_029

Benchmark 标识 `BENCH_06_00066_029` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/029`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_029`、`metric_029_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_06_00066_030

Benchmark 标识 `BENCH_06_00066_030` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/030`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_030`、`metric_030_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_06_00066_031

Benchmark 标识 `BENCH_06_00066_031` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/031`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_031`、`metric_031_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_06_00066_032

Benchmark 标识 `BENCH_06_00066_032` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/032`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_032`、`metric_032_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_06_00066_033

Benchmark 标识 `BENCH_06_00066_033` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/033`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_033`、`metric_033_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_06_00066_034

Benchmark 标识 `BENCH_06_00066_034` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/034`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_034`、`metric_034_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_06_00066_035

Benchmark 标识 `BENCH_06_00066_035` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/035`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_035`、`metric_035_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_06_00066_036

Benchmark 标识 `BENCH_06_00066_036` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/036`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_036`、`metric_036_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_06_00066_037

Benchmark 标识 `BENCH_06_00066_037` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/037`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_037`、`metric_037_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_06_00066_038

Benchmark 标识 `BENCH_06_00066_038` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/038`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_038`、`metric_038_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_06_00066_039

Benchmark 标识 `BENCH_06_00066_039` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/039`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_039`、`metric_039_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_06_00066_040

Benchmark 标识 `BENCH_06_00066_040` 属于 `bench-domain-06` 模块，接口路径是 `/api/benchmark/bench-domain-06/case/00066/040`。
核心表 `t_bench_bench_domain_06_66` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B066040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_06_00066_040`、`metric_040_value`、`t_bench_bench_domain_06_66` 三个关键词是否同时出现在上下文。
