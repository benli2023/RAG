---
title: Benchmark Document 00071
domain: bench-domain-11
type: reference
summary: Synthetic benchmark document 00071 for bench-domain-11. Tokens include BENCH_11_00071_001 BENCH_11_00071_002 BENCH_11_00071_003 BENCH_11_00071_004 BENCH_11_00071_005.
keywords:
  - BENCH_11_00071_001
  - BENCH_11_00071_002
  - BENCH_11_00071_003
  - BENCH_11_00071_004
  - BENCH_11_00071_005
  - BENCH_11_00071_006
  - BENCH_11_00071_007
  - BENCH_11_00071_008
  - BENCH_11_00071_009
  - BENCH_11_00071_010
  - BENCH_11_00071_011
  - BENCH_11_00071_012
  - BENCH_11_00071_013
  - BENCH_11_00071_014
  - BENCH_11_00071_015
  - BENCH_11_00071_016
  - BENCH_11_00071_017
  - BENCH_11_00071_018
  - BENCH_11_00071_019
  - BENCH_11_00071_020
  - BENCH_11_00071_021
  - BENCH_11_00071_022
  - BENCH_11_00071_023
  - BENCH_11_00071_024
  - BENCH_11_00071_025
  - BENCH_11_00071_026
  - BENCH_11_00071_027
  - BENCH_11_00071_028
  - BENCH_11_00071_029
  - BENCH_11_00071_030
  - BENCH_11_00071_031
  - BENCH_11_00071_032
  - BENCH_11_00071_033
  - BENCH_11_00071_034
  - BENCH_11_00071_035
  - BENCH_11_00071_036
  - BENCH_11_00071_037
  - BENCH_11_00071_038
  - BENCH_11_00071_039
  - BENCH_11_00071_040
  - bench-domain-11
  - benchmark
related_domains:
  - bench-domain-11
  - global
acl: public
---

# Benchmark Document 00071

This generated document belongs to `bench-domain-11` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_11_00071_001

Benchmark 标识 `BENCH_11_00071_001` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/001`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_001`、`metric_001_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_11_00071_002

Benchmark 标识 `BENCH_11_00071_002` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/002`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_002`、`metric_002_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_11_00071_003

Benchmark 标识 `BENCH_11_00071_003` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/003`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_003`、`metric_003_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_11_00071_004

Benchmark 标识 `BENCH_11_00071_004` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/004`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_004`、`metric_004_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_11_00071_005

Benchmark 标识 `BENCH_11_00071_005` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/005`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_005`、`metric_005_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_11_00071_006

Benchmark 标识 `BENCH_11_00071_006` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/006`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_006`、`metric_006_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_11_00071_007

Benchmark 标识 `BENCH_11_00071_007` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/007`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_007`、`metric_007_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_11_00071_008

Benchmark 标识 `BENCH_11_00071_008` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/008`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_008`、`metric_008_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_11_00071_009

Benchmark 标识 `BENCH_11_00071_009` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/009`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_009`、`metric_009_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_11_00071_010

Benchmark 标识 `BENCH_11_00071_010` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/010`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_010`、`metric_010_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_11_00071_011

Benchmark 标识 `BENCH_11_00071_011` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/011`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_011`、`metric_011_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_11_00071_012

Benchmark 标识 `BENCH_11_00071_012` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/012`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_012`、`metric_012_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_11_00071_013

Benchmark 标识 `BENCH_11_00071_013` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/013`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_013`、`metric_013_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_11_00071_014

Benchmark 标识 `BENCH_11_00071_014` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/014`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_014`、`metric_014_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_11_00071_015

Benchmark 标识 `BENCH_11_00071_015` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/015`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_015`、`metric_015_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_11_00071_016

Benchmark 标识 `BENCH_11_00071_016` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/016`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_016`、`metric_016_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_11_00071_017

Benchmark 标识 `BENCH_11_00071_017` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/017`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_017`、`metric_017_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_11_00071_018

Benchmark 标识 `BENCH_11_00071_018` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/018`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_018`、`metric_018_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_11_00071_019

Benchmark 标识 `BENCH_11_00071_019` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/019`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_019`、`metric_019_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_11_00071_020

Benchmark 标识 `BENCH_11_00071_020` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/020`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_020`、`metric_020_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_11_00071_021

Benchmark 标识 `BENCH_11_00071_021` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/021`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_021`、`metric_021_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_11_00071_022

Benchmark 标识 `BENCH_11_00071_022` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/022`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_022`、`metric_022_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_11_00071_023

Benchmark 标识 `BENCH_11_00071_023` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/023`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_023`、`metric_023_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_11_00071_024

Benchmark 标识 `BENCH_11_00071_024` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/024`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_024`、`metric_024_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_11_00071_025

Benchmark 标识 `BENCH_11_00071_025` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/025`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_025`、`metric_025_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_11_00071_026

Benchmark 标识 `BENCH_11_00071_026` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/026`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_026`、`metric_026_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_11_00071_027

Benchmark 标识 `BENCH_11_00071_027` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/027`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_027`、`metric_027_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_11_00071_028

Benchmark 标识 `BENCH_11_00071_028` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/028`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_028`、`metric_028_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_11_00071_029

Benchmark 标识 `BENCH_11_00071_029` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/029`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_029`、`metric_029_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_11_00071_030

Benchmark 标识 `BENCH_11_00071_030` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/030`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_030`、`metric_030_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_11_00071_031

Benchmark 标识 `BENCH_11_00071_031` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/031`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_031`、`metric_031_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_11_00071_032

Benchmark 标识 `BENCH_11_00071_032` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/032`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_032`、`metric_032_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_11_00071_033

Benchmark 标识 `BENCH_11_00071_033` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/033`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_033`、`metric_033_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_11_00071_034

Benchmark 标识 `BENCH_11_00071_034` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/034`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_034`、`metric_034_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_11_00071_035

Benchmark 标识 `BENCH_11_00071_035` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/035`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_035`、`metric_035_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_11_00071_036

Benchmark 标识 `BENCH_11_00071_036` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/036`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_036`、`metric_036_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_11_00071_037

Benchmark 标识 `BENCH_11_00071_037` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/037`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_037`、`metric_037_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_11_00071_038

Benchmark 标识 `BENCH_11_00071_038` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/038`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_038`、`metric_038_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_11_00071_039

Benchmark 标识 `BENCH_11_00071_039` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/039`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_039`、`metric_039_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_11_00071_040

Benchmark 标识 `BENCH_11_00071_040` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00071/040`。
核心表 `t_bench_bench_domain_11_71` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B071040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00071_040`、`metric_040_value`、`t_bench_bench_domain_11_71` 三个关键词是否同时出现在上下文。
