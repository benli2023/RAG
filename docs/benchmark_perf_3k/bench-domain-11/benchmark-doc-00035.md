---
title: Benchmark Document 00035
domain: bench-domain-11
type: reference
summary: Synthetic benchmark document 00035 for bench-domain-11. Tokens include BENCH_11_00035_001 BENCH_11_00035_002 BENCH_11_00035_003 BENCH_11_00035_004 BENCH_11_00035_005.
keywords:
  - BENCH_11_00035_001
  - BENCH_11_00035_002
  - BENCH_11_00035_003
  - BENCH_11_00035_004
  - BENCH_11_00035_005
  - BENCH_11_00035_006
  - BENCH_11_00035_007
  - BENCH_11_00035_008
  - BENCH_11_00035_009
  - BENCH_11_00035_010
  - BENCH_11_00035_011
  - BENCH_11_00035_012
  - BENCH_11_00035_013
  - BENCH_11_00035_014
  - BENCH_11_00035_015
  - BENCH_11_00035_016
  - BENCH_11_00035_017
  - BENCH_11_00035_018
  - BENCH_11_00035_019
  - BENCH_11_00035_020
  - BENCH_11_00035_021
  - BENCH_11_00035_022
  - BENCH_11_00035_023
  - BENCH_11_00035_024
  - BENCH_11_00035_025
  - BENCH_11_00035_026
  - BENCH_11_00035_027
  - BENCH_11_00035_028
  - BENCH_11_00035_029
  - BENCH_11_00035_030
  - BENCH_11_00035_031
  - BENCH_11_00035_032
  - BENCH_11_00035_033
  - BENCH_11_00035_034
  - BENCH_11_00035_035
  - BENCH_11_00035_036
  - BENCH_11_00035_037
  - BENCH_11_00035_038
  - BENCH_11_00035_039
  - BENCH_11_00035_040
  - bench-domain-11
  - benchmark
related_domains:
  - bench-domain-11
  - global
acl: public
---

# Benchmark Document 00035

This generated document belongs to `bench-domain-11` and is used only for retrieval performance benchmarking.

## 场景 001 BENCH_11_00035_001

Benchmark 标识 `BENCH_11_00035_001` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/001`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_001_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035001`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_001`、`metric_001_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 002 BENCH_11_00035_002

Benchmark 标识 `BENCH_11_00035_002` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/002`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_002_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035002`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_002`、`metric_002_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 003 BENCH_11_00035_003

Benchmark 标识 `BENCH_11_00035_003` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/003`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_003_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035003`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_003`、`metric_003_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 004 BENCH_11_00035_004

Benchmark 标识 `BENCH_11_00035_004` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/004`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_004_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035004`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_004`、`metric_004_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 005 BENCH_11_00035_005

Benchmark 标识 `BENCH_11_00035_005` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/005`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_005_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035005`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_005`、`metric_005_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 006 BENCH_11_00035_006

Benchmark 标识 `BENCH_11_00035_006` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/006`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_006_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035006`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_006`、`metric_006_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 007 BENCH_11_00035_007

Benchmark 标识 `BENCH_11_00035_007` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/007`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_007_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035007`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_007`、`metric_007_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 008 BENCH_11_00035_008

Benchmark 标识 `BENCH_11_00035_008` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/008`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_008_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035008`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_008`、`metric_008_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 009 BENCH_11_00035_009

Benchmark 标识 `BENCH_11_00035_009` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/009`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_009_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035009`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_009`、`metric_009_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 010 BENCH_11_00035_010

Benchmark 标识 `BENCH_11_00035_010` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/010`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_010_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035010`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_010`、`metric_010_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 011 BENCH_11_00035_011

Benchmark 标识 `BENCH_11_00035_011` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/011`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_011_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035011`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_011`、`metric_011_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 012 BENCH_11_00035_012

Benchmark 标识 `BENCH_11_00035_012` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/012`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_012_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035012`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_012`、`metric_012_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 013 BENCH_11_00035_013

Benchmark 标识 `BENCH_11_00035_013` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/013`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_013_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035013`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_013`、`metric_013_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 014 BENCH_11_00035_014

Benchmark 标识 `BENCH_11_00035_014` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/014`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_014_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035014`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_014`、`metric_014_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 015 BENCH_11_00035_015

Benchmark 标识 `BENCH_11_00035_015` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/015`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_015_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035015`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_015`、`metric_015_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 016 BENCH_11_00035_016

Benchmark 标识 `BENCH_11_00035_016` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/016`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_016_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035016`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_016`、`metric_016_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 017 BENCH_11_00035_017

Benchmark 标识 `BENCH_11_00035_017` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/017`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_017_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035017`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_017`、`metric_017_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 018 BENCH_11_00035_018

Benchmark 标识 `BENCH_11_00035_018` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/018`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_018_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035018`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_018`、`metric_018_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 019 BENCH_11_00035_019

Benchmark 标识 `BENCH_11_00035_019` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/019`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_019_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035019`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_019`、`metric_019_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 020 BENCH_11_00035_020

Benchmark 标识 `BENCH_11_00035_020` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/020`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_020_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035020`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_020`、`metric_020_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 021 BENCH_11_00035_021

Benchmark 标识 `BENCH_11_00035_021` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/021`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_021_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035021`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_021`、`metric_021_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 022 BENCH_11_00035_022

Benchmark 标识 `BENCH_11_00035_022` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/022`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_022_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035022`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_022`、`metric_022_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 023 BENCH_11_00035_023

Benchmark 标识 `BENCH_11_00035_023` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/023`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_023_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035023`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_023`、`metric_023_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 024 BENCH_11_00035_024

Benchmark 标识 `BENCH_11_00035_024` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/024`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_024_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035024`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_024`、`metric_024_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 025 BENCH_11_00035_025

Benchmark 标识 `BENCH_11_00035_025` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/025`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_025_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035025`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_025`、`metric_025_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 026 BENCH_11_00035_026

Benchmark 标识 `BENCH_11_00035_026` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/026`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_026_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035026`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_026`、`metric_026_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 027 BENCH_11_00035_027

Benchmark 标识 `BENCH_11_00035_027` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/027`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_027_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035027`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_027`、`metric_027_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 028 BENCH_11_00035_028

Benchmark 标识 `BENCH_11_00035_028` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/028`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_028_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035028`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_028`、`metric_028_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 029 BENCH_11_00035_029

Benchmark 标识 `BENCH_11_00035_029` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/029`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_029_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035029`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_029`、`metric_029_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 030 BENCH_11_00035_030

Benchmark 标识 `BENCH_11_00035_030` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/030`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_030_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035030`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_030`、`metric_030_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 031 BENCH_11_00035_031

Benchmark 标识 `BENCH_11_00035_031` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/031`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_031_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035031`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_031`、`metric_031_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 032 BENCH_11_00035_032

Benchmark 标识 `BENCH_11_00035_032` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/032`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_032_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035032`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_032`、`metric_032_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 033 BENCH_11_00035_033

Benchmark 标识 `BENCH_11_00035_033` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/033`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_033_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035033`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_033`、`metric_033_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 034 BENCH_11_00035_034

Benchmark 标识 `BENCH_11_00035_034` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/034`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_034_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035034`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_034`、`metric_034_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 035 BENCH_11_00035_035

Benchmark 标识 `BENCH_11_00035_035` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/035`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_035_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035035`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_035`、`metric_035_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 036 BENCH_11_00035_036

Benchmark 标识 `BENCH_11_00035_036` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/036`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_036_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035036`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_036`、`metric_036_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 037 BENCH_11_00035_037

Benchmark 标识 `BENCH_11_00035_037` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/037`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_037_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035037`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_037`、`metric_037_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 038 BENCH_11_00035_038

Benchmark 标识 `BENCH_11_00035_038` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/038`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_038_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035038`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_038`、`metric_038_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 039 BENCH_11_00035_039

Benchmark 标识 `BENCH_11_00035_039` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/039`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_039_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035039`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_039`、`metric_039_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。

## 场景 040 BENCH_11_00035_040

Benchmark 标识 `BENCH_11_00035_040` 属于 `bench-domain-11` 模块，接口路径是 `/api/benchmark/bench-domain-11/case/00035/040`。
核心表 `t_bench_bench_domain_11_35` 必须写入字段 `metric_040_value`，并保留 trace_id、tenant_id 和 version。
如果校验失败，服务返回错误码 `B035040`，调用方需要记录审计日志并触发补偿任务。
排障时优先检查 `BENCH_11_00035_040`、`metric_040_value`、`t_bench_bench_domain_11_35` 三个关键词是否同时出现在上下文。
